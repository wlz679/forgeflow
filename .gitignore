node_modules
.mcp.json
.worktrees

dist/
public/og/
.env
.env.local

# Astro dev/build cache (auto-generated)
.astro/astro/

# Superpowers scratch dirs (brainstorm + sdd runtime). P70 untracked all
# historically-tracked scratch files; never `git add` files under this path.
.superpowers/
docs/superpowers/_research/

# Feishu MCP scratch (SDD subagent batch downloads). Never commit; ephemeral.
.feishu-batch/

# Audit script runtime outputs (scripts/p72-findings-final.json etc.). Source
# scripts under scripts/ are committed; their JSON outputs are not.
scripts/p72-findings-final.json

# Twemoji PNG cache (scripts/.twemoji-cache/) — populated on first build,
# never commit binaries. Re-seed with `pnpm build:og` if cleared.
scripts/.twemoji-cache/

# Per-batch merge scratch (merge_batch1.mjs, extract_entries.mjs, etc.) — local
# staging only. Sources are in src/data/, build artifacts in dist/; tmp is ephemeral.
tmp/

.codegraph/
.claude/
CLAUDE.md
.serena/