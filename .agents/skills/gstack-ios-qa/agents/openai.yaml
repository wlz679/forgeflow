interface:
  display_name: "gstack-ios-qa"
  short_description: "Live-device iOS QA for SwiftUI apps. Connects to a real iPhone via USB CoreDevice IPv6 tunnel, reads Swift source to..."
  default_prompt: "Use gstack-ios-qa for this task."
policy:
  allow_implicit_invocation: true
