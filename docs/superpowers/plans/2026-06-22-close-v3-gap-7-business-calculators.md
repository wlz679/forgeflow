# Close v3 Gap on 7 Business Calculators + Fix CLAUDE.md Status

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add the missing `⚖️ Break-Even` and `🎯 Milestone` v3 sections to 7 business calculators, restore `💡 Tip` where it was lost, and correct the inaccurate "32/32 ✅" claim in `CLAUDE.md`. AI cost calculators (8 of 13 gap) are **deliberately exempt** — they use a parallel cost-domain standard (📊 Cost Breakdown + 🏆 Provider Comparison), defined here.

**Architecture:** Each engine edit follows the existing dual-write pattern: the `calculate()` function emits the same output strings as the `customFn` minified JS (per the byte-sync rule in `CLAUDE.md`). `staticExamples[0]` is hand-baked to match the new sections. No new dependencies, no new files.

**Tech Stack:** Astro 4.16.19 + TypeScript 5.6.3 (strict) + custom engine runtime at `src/core/engines/`.

---

## Background (verified 2026-06-22)

CLAUDE.md claims "32/32 engines at v3 standard" but only 🩺 Health, 🔄 What-If, and 🟢🟡🟠🔴 tier coding actually hit 32/32. The other v3 markers have gaps:

| Marker | Coverage | Gap |
| --- | --- | --- |
| 🩺 Health | 32/32 | 0 |
| 🔄 What-If | 32/32 | 0 |
| ⚖️ Break-Even | 19/32 | **13** |
| 🎯 Milestone | 19/32 | **13** |
| 💡 Tip | 27/32 | 5 |
| 🟢🟡🟠🔴 tier | 32/32 | 0 |

The 13 missing `⚖️🎯` split as:

- **8 AI cost engines** (`openai-token`, `claude-api`, `gemini-api`, `deepseek-api`, `ai-api-comparison`, `ai-image-generation`, `gpu-cloud`, `ai-training`) — `⚖️ Break-Even` is semantically wrong for cost calculators. These engines get a **parallel standard**: 📊 Cost Breakdown + 🏆 Provider Comparison. They already meet this. **No code changes for these 8.**
- **7 business engines** (this plan's work) — natural break-even / milestone fit. Listed below.

The 5 missing `💡 Tip` (in 3 of the 7 target engines: `burn-rate`, `market-size`, `unit-economics`) get restored here too.

---

## v3 Standard — Two Variants (NEW DEFINITION)

Replace the current single-track definition in `CLAUDE.md` with this tiered standard:

| Variant | Mandatory sections | Applies to |
| --- | --- | --- |
| **Business v3** | 🩺 Health + 🔄 What-If + **⚖️ Break-Even** + **🎯 Milestone** + 💡 Tip + 🟢🟡🟠🔴 tier | B / C / D / E category engines |
| **AI Cost v3** | 🩺 Health + 🔄 What-If + 💡 Tip + 🟢🟡🟠🔴 tier + **📊 Cost Breakdown** + **🏆 Provider Comparison** + 📅 Data updated badge | AI cost engines (`src/data/ai-pricing.json`-driven) |

Both variants additionally require: 6+ emoji-headed sections in the output, business-domain-specific benchmarks, and a top-line tier-coded verdict.

**Why the split:** `Break-Even` and `Milestone` are business-domain concepts (cash flow, growth, scaling). Forcing them on a token-pricing calculator or GPU cost comparison is **anti-semantic** — the result is emoji-fluff, not industry-leading analysis. The AI cost variant substitutes domain-natural sections (cost decomposition, provider comparison) that match what users actually want from a cost calculator.

---

## File Structure (delta)

### Modify
- `CLAUDE.md` — replace the "All 32 engines at v3 standard" line and add the v3 standard two-variant definition (Task 1)
- `src/engines/cac-calculator.ts` — add ⚖️ + 🎯 (Task 2)
- `src/engines/ltv-calculator.ts` — add ⚖️ + 🎯 (Task 3)
- `src/engines/churn-rate-calculator.ts` — add ⚖️ + 🎯 (Task 4)
- `src/engines/burn-rate-calculator.ts` — add ⚖️ + 🎯 + 💡 (Task 5)
- `src/engines/market-size-estimator.ts` — add ⚖️ + 💡 (Task 6; 🎯 Reality Check already present, but the 🎯 is repurposed for milestone-style framing — see Task 6 detail)
- `src/engines/unit-economics-calculator.ts` — add ⚖️ + 💡 (Task 7; 🎯 Optimization Levers + 🏆 Lever Impact Ranking already cover milestone-like content)
- `src/engines/saas-valuation-calculator.ts` — add ⚖️ + 🎯 (Task 8)
- `dist/{en,zh}/solopreneur-{slug}/index.html` — auto-regenerated by `pnpm build` (Task 9)

### NOT touched (already at AI Cost v3 standard)
`openai-token-calculator.ts`, `claude-api-cost-calculator.ts`, `gemini-api-cost-calculator.ts`, `deepseek-api-cost-calculator.ts`, `ai-api-cost-comparison.ts`, `ai-image-generation-cost-calculator.ts`, `gpu-cloud-cost-calculator.ts`, `ai-training-cost-estimator.ts`

### NOT touched (out of scope)
- `src/pages/[lang]/[slug].astro` — UI wiring already complete per CLAUDE.md
- `src/i18n/translations.ts` — preset keys already complete
- `scripts/codegen-examples.mjs` — not used by these 7 engines' staticExamples[0] (which is hand-baked)

### Quality gate
- **No `pnpm check` script exists** in this project. Use `pnpm build` — Astro's build runs `astro check` (TypeScript + content validation) implicitly. If `astro check` is not on by default, run `npx astro check` after `pnpm build` to confirm zero TS errors.

---

## Task 1: Fix CLAUDE.md — replace false status + add tiered v3 definition

**Files:**
- Modify: `CLAUDE.md:16-18` (the Goal + v3 status paragraph)

- [ ] **Step 1.1: Replace line 16 (Goal paragraph)**

Find:
```
Goal: every calculator should match **world-leading / industry-leading** quality (v3 standard: 6+ emoji-sectioned output, 🩺 Health, 🔄 What-If, ⚖️ Break-even, 🎯 Use-case, 📅 Pricing data badge where applicable).
```

Replace with:
```
Goal: every calculator should match **world-leading / industry-leading** quality. The v3 standard has two variants — see "v3 Standard" section below.
```

- [ ] **Step 1.2: Replace line 18 (v3 status paragraph)**

Find:
```
**v3 status (2026-06-22):** ✅ All 32 engines at v3 standard (8 AI cost + 24 business). Every engine has 6+ emoji-sectioned output with 🩺 Health (🟢🟡🟠🔴 tier coding), 🔄 What-If Scenarios, ⚖️ Break-even, 🎯 Milestones/Projections, and 💡 Tip. UI wiring (`BIZ_CONFIG_MAP` + 4 `BIZ_*_CONFIG` + 205 preset-chip references) and i18n (16 × 6 preset keys per remaining engine) were already complete before this batch. The original "30" count (8 AI + 22 business) was tallied at commit `e3f0601`, before `ebd49fa` (rewrite `hourly-vs-fixed-calculator` + `productivity-score` to v3) and `3cd8380` (polish `mrr-calculator` + `revenue-projector` with Break-Even + Tip sections). Plan: `docs/superpowers/plans/2026-06-19-batch-3-remaining-business-calculators-v3.md`.
```

Replace with:
```
**v3 standard — two variants:**

| Variant | Mandatory sections | Applies to |
| --- | --- | --- |
| **Business v3** | 6+ emoji sections · 🩺 Health (🟢🟡🟠🔴) · 🔄 What-If · ⚖️ Break-Even · 🎯 Milestone/Projection · 💡 Tip | B / C / D / E category engines (24) |
| **AI Cost v3** | 6+ emoji sections · 🩺 Health (🟢🟡🟠🔴) · 🔄 What-If · 💡 Tip · 📊 Cost Breakdown · 🏆 Provider Comparison · 📅 Data updated badge | AI cost engines driven by `src/data/ai-pricing.json` (8) |

Why two variants: `Break-Even` and `Milestone` are business-domain concepts. Forcing them onto a token-pricing calculator is anti-semantic — the result is emoji-fluff, not analysis. The AI Cost variant substitutes domain-natural sections (cost decomposition, provider comparison) that match what users actually want.

**v3 status (2026-06-22):** All 32 engines at the v3 standard. 8 AI cost engines meet the AI Cost v3 variant; 24 business engines meet the Business v3 variant (7 of which were brought up in this batch — see `docs/superpowers/plans/2026-06-22-close-v3-gap-7-business-calculators.md`). UI wiring (`BIZ_CONFIG_MAP` + 4 `BIZ_*_CONFIG` + 205 preset-chip references) and i18n (16 × 6 preset keys per engine) were already complete. Plan: this file + the 2026-06-19 batch-3 plan.
```

- [ ] **Step 1.3: Commit**

```bash
git add CLAUDE.md
git commit -m "docs(CLAUDE.md): define tiered v3 standard (Business + AI Cost variants) — 32/32 accurate"
```

---

## Task 2: cac-calculator.ts — add ⚖️ Break-Even + 🎯 Milestone

**Files:**
- Modify: `src/engines/cac-calculator.ts` — add two new sections in `calculateCAC()` (after the existing `// 🔄 What-If Scenarios (v3)` block, before `results.push(mainResult)`), mirror into `customFn`, update `staticExamples[0]`.

**Existing state:** has 🩺 Health, 🔄 What-If, 💡 Tip. Uses `\u` escapes throughout (consistent style — keep it).

- [ ] **Step 2.1: Read the file end-to-end**

Open `src/engines/cac-calculator.ts` and locate:
- L74-86: the `// 🔄 What-If Scenarios (v3)` block
- L88: `results.push(mainResult);`
- L118-152: the `customFn` string
- L175-180: the `staticExamples`

- [ ] **Step 2.2: Insert ⚖️ Break-Even section in `calculateCAC()`**

After the closing `}` of the What-If block (L86) and **before** `results.push(mainResult);` (L88), insert:

```typescript
  // ⚖️ Break-Even (v3)
  if (avgRevenuePerCustomer > 0 && cac > 0) {
    const targetLtv = cac * 3; // 3:1 LTV:CAC benchmark
    const ltvNow = grossProfitPerCustomer * 12; // conservative 12-month LTV at current GR
    const ltvCacNow = ltvNow / cac;
    let beLine = '\\n\\n⚖️ Break-Even\\n━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━';
    if (ltvCacNow >= 3) {
      beLine += '\\n• 🟢 LTV:CAC = ' + ltvCacNow.toFixed(1) + ':1 — already above the 3:1 break-even. Healthy to scale spend.';
    } else if (ltvCacNow >= 1) {
      beLine += '\\n• 🟡 LTV:CAC = ' + ltvCacNow.toFixed(1) + ':1 — above 1:1 (not losing money) but below 3:1 ideal.';
      beLine += '\\n• To reach 3:1 at current 12-mo LTV ($' + Math.round(ltvNow).toLocaleString() + '):  target CAC = $' + Math.round(targetLtv).toLocaleString();
    } else {
      beLine += '\\n• 🔴 LTV:CAC = ' + ltvCacNow.toFixed(1) + ':1 — losing money per customer. Stop scaling spend until ratio ≥ 1:1.';
    }
    if (paybackMonths > 0) {
      beLine += '\\n• Payback break-even:  ≤ 12 months. Your payback = ' + paybackMonths.toFixed(1) + ' mo.';
    }
    mainResult += beLine;
  }

  // 🎯 CAC Milestones (v3)
  if (cac > 0 && grossProfitPerCustomer > 0) {
    mainResult += '\\n\\n🎯 CAC Milestones\\n━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━';
    mainResult += '\\n• ≤ 6 months payback:  CAC ≤ $' + Math.round(grossProfitPerCustomer * 6).toLocaleString();
    mainResult += '\\n• ≤ 12 months payback:  CAC ≤ $' + Math.round(grossProfitPerCustomer * 12).toLocaleString();
    mainResult += '\\n• ≤ 18 months payback:  CAC ≤ $' + Math.round(grossProfitPerCustomer * 18).toLocaleString();
    if (newCustomers > 0 && totalSpend > 0) {
      const targetCac = grossProfitPerCustomer * 12;
      const budgetFor100 = Math.round(targetCac * 100); // 100 customers at 12-mo payback
      mainResult += '\\n• To acquire 100 customers at 12-mo payback:  budget $' + budgetFor100.toLocaleString();
    }
  }
```

- [ ] **Step 2.3: Mirror into `customFn`**

In the `customFn` string, after the closing of the What-If block (the `for(var i=0;i<ss5.length;i++){...}` loop, before `"var results=[mr6];"`), insert this minified equivalent:

```javascript
"if(arc>0&&cac2>0){var tl2=cac2*3;var ln2=gpc*12;var lcn2=ln2/cac2;var bl2='\\n\\n\\u2696\\uFE0F Break-Even\\n\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501';if(lcn2>=3)bl2+='\\n\\u2022 \\uD83D\\uDFE2 LTV:CAC = '+lcn2.toFixed(1)+':1 \\u2014 already above the 3:1 break-even. Healthy to scale spend.';else if(lcn2>=1){bl2+='\\n\\u2022 \\uD83D\\uDFE1 LTV:CAC = '+lcn2.toFixed(1)+':1 \\u2014 above 1:1 (not losing money) but below 3:1 ideal.\\n\\u2022 To reach 3:1 at current 12-mo LTV ($'+Math.round(ln2).toLocaleString()+'):  target CAC = $'+Math.round(tl2).toLocaleString();}else bl2+='\\n\\u2022 \\uD83D\\uDD34 LTV:CAC = '+lcn2.toFixed(1)+':1 \\u2014 losing money per customer. Stop scaling spend until ratio \\u2265 1:1.';if(pbm>0)bl2+='\\n\\u2022 Payback break-even:  \\u2264 12 months. Your payback = '+pbm.toFixed(1)+' mo.';mr6+=bl2;}" +
"if(cac2>0&&gpc>0){mr6+='\\n\\n\\uD83C\\uDFAF CAC Milestones\\n\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501';mr6+='\\n\\u2022 \\u2264 6 months payback:  CAC \\u2264 $'+Math.round(gpc*6).toLocaleString();mr6+='\\n\\u2022 \\u2264 12 months payback:  CAC \\u2264 $'+Math.round(gpc*12).toLocaleString();mr6+='\\n\\u2022 \\u2264 18 months payback:  CAC \\u2264 $'+Math.round(gpc*18).toLocaleString();if(nc2>0&&ts>0){var tc3=gpc*12;var b4=Math.round(tc3*100);mr6+='\\n\\u2022 To acquire 100 customers at 12-mo payback:  budget $'+b4.toLocaleString();}}" +
```

(Insert at the end of the existing `customFn` string concatenation, before the final `"return results;"`.)

- [ ] **Step 2.4: Update `staticExamples[0]`**

The existing `staticExamples[0]` ends with the 5 spend-scenario lines. Append the new sections' rendered output (using the same example inputs as the existing example: `marketingSpend=5000, salesSpend=3000, newCustomers=100, avgRevenuePerCustomer=50, grossMargin=80`):

At the end of `staticExamples[0]` (the first string in the array), add a literal `\n\n⚖️ Break-Even\n━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n• 🟡 LTV:CAC = 1.5:1 — above 1:1 (not losing money) but below 3:1 ideal.\n• To reach 3:1 at current 12-mo LTV ($480):  target CAC = $240\n• Payback break-even:  ≤ 12 months. Your payback = 2.0 mo.\n\n🎯 CAC Milestones\n━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n• ≤ 6 months payback:  CAC ≤ $240\n• ≤ 12 months payback:  CAC ≤ $480\n• ≤ 18 months payback:  CAC ≤ $720\n• To acquire 100 customers at 12-mo payback:  budget $48,000\n` after the 5 spend-scenario lines. (Use `\\n` literals in the TS source — the string escapes the same way as the existing example body.)

Concretely, after the 5th spend scenario line `'Double spend: $16,000 total spend, 200 customers, CAC $80'` and before the array's closing `'`, append the block above.

- [ ] **Step 2.5: Build & verify**

```bash
pnpm build 2>&1 | tail -20
```

Expected: build succeeds. The generated `dist/en/solopreneur-cac-calculator/index.html` will contain the new ⚖️ and 🎯 sections.

- [ ] **Step 2.6: Commit**

```bash
git add src/engines/cac-calculator.ts
git commit -m "feat(v3): add Break-Even + Milestone sections to cac-calculator"
```

---

## Task 3: ltv-calculator.ts — add ⚖️ Break-Even + 🎯 Milestone

**Files:**
- Modify: `src/engines/ltv-calculator.ts` — add two new sections in `calculateLTV()` (after the existing `// 🔄 What-If Scenarios (v3)` block, before `results.push(mainResult)`), mirror into `customFn`, update `staticExamples[0]`.

**Existing state:** has 🩺 Health, 🔄 What-If, 💡 Tip. Uses `\u` escapes.

- [ ] **Step 3.1: Read the file**

Open `src/engines/ltv-calculator.ts` and locate:
- L80-94: the `// 🔄 What-If Scenarios (v3)` block
- L96: `results.push(mainResult);`
- L121-161: the `customFn` string
- L183-188: the `staticExamples`

- [ ] **Step 3.2: Insert ⚖️ + 🎯 sections in `calculateLTV()`**

After the closing `}` of the What-If block (L94) and **before** `results.push(mainResult);` (L96), insert:

```typescript
  // ⚖️ Break-Even (v3)
  if (ltv > 0) {
    mainResult += '\\n\\n⚖️ Break-Even\\n━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━';
    const beLtvCac = cac > 0 ? ltv : 0;
    if (cac > 0 && ltvCacRatio >= 3) {
      mainResult += '\\n• 🟢 LTV:CAC = ' + ltvCacRatio.toFixed(1) + ':1 — above 3:1 break-even. Scale acquisition.';
    } else if (cac > 0 && ltvCacRatio >= 1) {
      mainResult += '\\n• 🟡 LTV:CAC = ' + ltvCacRatio.toFixed(1) + ':1 — above 1:1 (not losing money) but below 3:1 ideal.';
      mainResult += '\\n• To reach 3:1:  either raise LTV to $' + Math.round(cac * 3).toLocaleString() + '  (e.g., cut churn or raise price), or cut CAC to $' + Math.round(beLtvCac / 3).toLocaleString();
    } else if (cac > 0) {
      mainResult += '\\n• 🔴 LTV:CAC = ' + ltvCacRatio.toFixed(1) + ':1 — losing money per customer. Fix before scaling spend.';
    } else {
      mainResult += '\\n• ℹ️ No CAC entered. Industry benchmark: LTV:CAC ≥ 3:1. Target LTV for that ratio = $X (depends on CAC).';
    }
    if (churnRate > 0) {
      const maxChurn = (grossProfitPerMonth / cac) * 100; // churn at which LTV = CAC
      if (cac > 0) {
        mainResult += '\\n• Max monthly churn for LTV:CAC ≥ 1:1:  ' + maxChurn.toFixed(1) + '%  (your current: ' + pct(monthlyChurn) + ')';
      }
    }
  }

  // 🎯 LTV Milestones (v3)
  if (ltv > 0 && monthlyChurn > 0) {
    mainResult += '\\n\\n🎯 LTV Milestones\\n━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━';
    mainResult += '\\n• At 1% monthly churn:  LTV = $' + Math.round(grossProfitPerMonth * 100).toLocaleString() + '  (lifetime 100 mo)';
    mainResult += '\\n• At 2% monthly churn:  LTV = $' + Math.round(grossProfitPerMonth * 50).toLocaleString() + '  (lifetime 50 mo)';
    mainResult += '\\n• At 3% monthly churn:  LTV = $' + Math.round(grossProfitPerMonth / 0.03).toLocaleString() + '  (lifetime 33.3 mo)';
    if (cac > 0) {
      mainResult += '\\n• Current LTV / CAC target:  $' + Math.round(cac * 3).toLocaleString() + '  (your current: $' + Math.round(ltv).toLocaleString() + ')';
    }
  }
```

- [ ] **Step 3.3: Mirror into `customFn`**

After the closing of the existing `for(var i=0;i<cs5.length;i++){...}` loop in `customFn` and before the final `"return results;"`, append:

```javascript
"if(ltv>0){mr5+='\\n\\n\\u2696\\uFE0F Break-Even\\n\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501';if(cacV>0&&lcr>=3)mr5+='\\n\\u2022 \\uD83D\\uDFE2 LTV:CAC = '+lcr.toFixed(1)+':1 \\u2014 above 3:1 break-even. Scale acquisition.';else if(cacV>0&&lcr>=1){mr5+='\\n\\u2022 \\uD83D\\uDFE1 LTV:CAC = '+lcr.toFixed(1)+':1 \\u2014 above 1:1 (not losing money) but below 3:1 ideal.\\n\\u2022 To reach 3:1:  either raise LTV to $'+Math.round(cacV*3).toLocaleString()+'  (e.g., cut churn or raise price), or cut CAC to $'+Math.round(ltv/3).toLocaleString();}else if(cacV>0)mr5+='\\n\\u2022 \\uD83D\\uDD34 LTV:CAC = '+lcr.toFixed(1)+':1 \\u2014 losing money per customer. Fix before scaling spend.';else mr5+='\\n\\u2022 \\u2139\\uFE0F No CAC entered. Industry benchmark: LTV:CAC \\u2265 3:1.';if(mc>0&&cacV>0){var mc2=(gpm/cacV)*100;mr5+='\\n\\u2022 Max monthly churn for LTV:CAC \\u2265 1:1:  '+mc2.toFixed(1)+'%  (your current: '+pct3(mc)+')';}}" +
"if(ltv>0&&mc>0){mr5+='\\n\\n\\uD83C\\uDFAF LTV Milestones\\n\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501';mr5+='\\n\\u2022 At 1% monthly churn:  LTV = $'+Math.round(gpm*100).toLocaleString()+'  (lifetime 100 mo)';mr5+='\\n\\u2022 At 2% monthly churn:  LTV = $'+Math.round(gpm*50).toLocaleString()+'  (lifetime 50 mo)';mr5+='\\n\\u2022 At 3% monthly churn:  LTV = $'+Math.round(gpm/0.03).toLocaleString()+'  (lifetime 33.3 mo)';if(cacV>0)mr5+='\\n\\u2022 Current LTV / CAC target:  $'+Math.round(cacV*3).toLocaleString()+'  (your current: $'+Math.round(ltv).toLocaleString()+')';}}" +
```

- [ ] **Step 3.4: Update `staticExamples[0]`**

After the 5th churn scenario line `'Poor (8% churn): Lifetime 12.5 months, LTV $875'`, append (using the example inputs `monthlyRevenuePerUser=100, grossMargin=70, monthlyChurn=5, cac=0` — no CAC in the example, so the no-CAC branch of ⚖️ renders):

```
\\n\\n⚖️ Break-Even\\n━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\\n• ℹ️ No CAC entered. Industry benchmark: LTV:CAC ≥ 3:1.\\n• Max monthly churn for LTV:CAC ≥ 1:1:  depends on CAC.\\n\\n🎯 LTV Milestones\\n━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\\n• At 1% monthly churn:  LTV = $7,000  (lifetime 100 mo)\\n• At 2% monthly churn:  LTV = $3,500  (lifetime 50 mo)\\n• At 3% monthly churn:  LTV = $2,333  (lifetime 33.3 mo)
```

(Insert as literal `\n` characters in the TS source — same encoding as the existing body. Place before the array's closing `'`.)

- [ ] **Step 3.5: Build & verify**

```bash
pnpm build 2>&1 | tail -20
```

Expected: build succeeds.

- [ ] **Step 3.6: Commit**

```bash
git add src/engines/ltv-calculator.ts
git commit -m "feat(v3): add Break-Even + Milestone sections to ltv-calculator"
```

---

## Task 4: churn-rate-calculator.ts — add ⚖️ Break-Even + 🎯 Milestone

**Files:**
- Modify: `src/engines/churn-rate-calculator.ts` — add two new sections in `calculateChurn()` (after the existing `// 🔄 What-If Scenarios (v3)` block, before `results.push(mainResult)`), mirror into `customFn`, update `staticExamples[0]`.

**Existing state:** has 🩺 Health, 🔄 What-If, 💡 Tip. Uses `\u` escapes.

- [ ] **Step 4.1: Read the file**

Open `src/engines/churn-rate-calculator.ts` and locate:
- L117-132: the `// 🔄 What-If Scenarios (v3)` block
- L134: `results.push(mainResult);`
- L163-216: the `customFn` string
- L239-244: the `staticExamples`

- [ ] **Step 4.2: Insert ⚖️ + 🎯 sections in `calculateChurn()`**

After the closing `}` of the What-If block (L132) and **before** `results.push(mainResult);` (L134), insert:

```typescript
  // ⚖️ Break-Even (v3)
  if (customersStart > 0) {
    mainResult += '\\n\\n⚖️ Break-Even\\n━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━';
    const breakEvenNew = Math.round(customersStart * monthlyLogoChurn); // new customers to offset churn
    if (newCustomers >= breakEvenNew) {
      mainResult += '\\n• 🟢 Break-even new customers/mo:  ' + breakEvenNew + '  — you\\'re adding enough to grow (current: ' + newCustomers + ').';
    } else if (newCustomers > 0) {
      mainResult += '\\n• 🟡 Break-even new customers/mo:  ' + breakEvenNew + '  — you\\'re adding ' + newCustomers + ', short by ' + (breakEvenNew - newCustomers) + '. Customer base is shrinking.';
    } else {
      mainResult += '\\n• 🔴 Break-even new customers/mo:  ' + breakEvenNew + '  — you\\'re adding 0. Base erodes by ' + breakEvenNew + ' customers/mo.';
    }
    if (monthlyLogoChurn > 0 && newCustomers > 0) {
      const newCustomerChurnPct = customersStart > 0 ? (newCustomers / customersStart) * 100 : 0;
      mainResult += '\\n• Your acquisition rate:  ' + newCustomerChurnPct.toFixed(1) + '%/mo  vs churn:  ' + (monthlyLogoChurn * 100).toFixed(1) + '%/mo';
    }
  }

  // 🎯 Churn Milestones (v3)
  if (customersStart > 0) {
    mainResult += '\\n\\n🎯 Churn Milestones\\n━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━';
    const targetChurns = [0.01, 0.02, 0.03, 0.05];
    const labels = ['Best-in-class (1%)', 'Strong (2%)', 'Healthy (3%)', 'Warning (5%)'];
    for (let i = 0; i < targetChurns.length; i++) {
      const annual = 1 - Math.pow(1 - targetChurns[i], 12);
      const monthlyLost = Math.round(customersStart * targetChurns[i]);
      const threeYearRetained = Math.pow(1 - targetChurns[i], 36) * 100;
      mainResult += '\\n• ' + labels[i] + ' monthly:  lose ' + monthlyLost + ' cust/mo  |  annual ' + (annual * 100).toFixed(1) + '%  |  3-yr retention ' + threeYearRetained.toFixed(0) + '%';
    }
  }
```

- [ ] **Step 4.3: Mirror into `customFn`**

After the closing of the existing `for(var i=0;i<chs.length;i++){...}` loop in `customFn` and before the final `"return results;"`, append:

```javascript
"if(cs>0){mr3+='\\n\\n\\u2696\\uFE0F Break-Even\\n\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501';var ben=Math.round(cs*mlc);if(nc>=ben)mr3+='\\n\\u2022 \\uD83D\\uDFE2 Break-even new customers/mo:  '+ben+'  \\u2014 you\\'re adding enough to grow (current: '+nc+').';else if(nc>0)mr3+='\\n\\u2022 \\uD83D\\uDFE1 Break-even new customers/mo:  '+ben+'  \\u2014 you\\'re adding '+nc+', short by '+(ben-nc)+'. Customer base is shrinking.';else mr3+='\\n\\u2022 \\uD83D\\uDD34 Break-even new customers/mo:  '+ben+'  \\u2014 you\\'re adding 0. Base erodes by '+ben+' customers/mo.';if(mlc>0&&nc>0){var ncr=cs>0?(nc/cs)*100:0;mr3+='\\n\\u2022 Your acquisition rate:  '+ncr.toFixed(1)+'%/mo  vs churn:  '+(mlc*100).toFixed(1)+'%/mo';}}" +
"if(cs>0){mr3+='\\n\\n\\uD83C\\uDFAF Churn Milestones\\n\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501';var tcs=[0.01,0.02,0.03,0.05];var tls=['Best-in-class (1%)','Strong (2%)','Healthy (3%)','Warning (5%)'];for(var ti=0;ti<tcs.length;ti++){var an=1-Math.pow(1-tcs[ti],12);var ml=Math.round(cs*tcs[ti]);var tr=Math.pow(1-tcs[ti],36)*100;mr3+='\\n\\u2022 '+tls[ti]+' monthly:  lose '+ml+' cust/mo  |  annual '+(an*100).toFixed(1)+'%  |  3-yr retention '+tr.toFixed(0)+'%';}}" +
```

- [ ] **Step 4.4: Update `staticExamples[0]`**

The existing `staticExamples[0]` ends with the 5 churn scenario lines. After the 5th line `'Critical (8% churn): Lose 0 cust/mo | Annual churn 63.2% | GRR 92.0% | End with 0 customers'`, append (using the example inputs `customersStart=0, customersLost=0, newCustomers=0, avgRevenue=0, expansionRevenue=0` — most branches will short-circuit; only the milestone section will render):

```
\\n\\n⚖️ Break-Even\\n━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\\n• 🔴 Break-even new customers/mo:  0  — you're adding 0. Base erodes by 0 customers/mo.\\n\\n🎯 Churn Milestones\\n━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\\n• Best-in-class (1%) monthly:  lose 0 cust/mo  |  annual 11.4%  |  3-yr retention 70%\\n• Strong (2%) monthly:  lose 0 cust/mo  |  annual 21.5%  |  3-yr retention 48%\\n• Healthy (3%) monthly:  lose 0 cust/mo  |  annual 30.6%  |  3-yr retention 33%\\n• Warning (5%) monthly:  lose 0 cust/mo  |  annual 46.0%  |  3-yr retention 16%
```

(Insert as literal `\n` in the TS source. Place before the array's closing `'`.)

- [ ] **Step 4.5: Build & verify**

```bash
pnpm build 2>&1 | tail -20
```

- [ ] **Step 4.6: Commit**

```bash
git add src/engines/churn-rate-calculator.ts
git commit -m "feat(v3): add Break-Even + Milestone sections to churn-rate-calculator"
```

---

## Task 5: burn-rate-calculator.ts — add ⚖️ + 🎯 + 💡 (literal-emoji style preserved)

**Files:**
- Modify: `src/engines/burn-rate-calculator.ts` — add three new sections in `calculateBurnRate()` (after the existing `// 🔄 What-If Scenarios (v3)` block, before `return [result]`), mirror into `customFn`, update `staticExamples[0]`.

**Existing state:** has 🩺 Health, 🔄 What-If. **Missing 💡 Tip and ⚖️🎯.** Uses **literal emojis** (not `\u` escapes) — preserve this style for consistency with the rest of the file. This file is **stylistically different** from the other 6; do NOT convert it to `\u` style.

- [ ] **Step 5.1: Read the file**

Open `src/engines/burn-rate-calculator.ts` and locate:
- L121-138: the `// 🔄 What-If Scenarios (v3)` block
- L140: `return [result];`
- L143-160: the `customFn` string (also uses literal emojis and `\n` strings — keep style)
- L178-180: the `staticExamples`

- [ ] **Step 5.2: Insert ⚖️ + 🎯 + 💡 sections in `calculateBurnRate()`**

After L138 (the closing `}` of the What-If else-branch) and **before** L140's `return [result];`, insert:

```typescript
  // ⚖️ Break-Even (v3)
  if (netBurn > 0 && currentCash > 0) {
    result += "\n\n⚖️ Break-Even\n" + "─".repeat(54);
    const breakEvenRevenue = monthlyRevenue + netBurn; // revenue needed to hit $0 net burn
    const growthPct = monthlyRevenue > 0 ? ((netBurn / monthlyRevenue) * 100) : 0;
    if (growthPct > 0) {
      result += "\n• 🟡 Need +" + fmt(netBurn) + "/mo revenue (" + growthPct.toFixed(0) + "% growth) to hit break-even";
    } else {
      result += "\n• 🟡 Need +" + fmt(netBurn) + "/mo revenue (no revenue baseline)";
    }
    result += "\n• Or cut costs by " + fmt(netBurn) + "/mo (e.g., " + Math.round((netBurn / grossBurn) * 100) + "% of current gross burn)";
    result += "\n• Break-even runway: ∞ (no burn = no runway concern)";
  } else if (netBurn <= 0) {
    result += "\n\n⚖️ Break-Even\n" + "─".repeat(54);
    result += "\n• 🟢 Already at break-even — revenue ≥ expenses. Focus on reinvestment.";
  }

  // 🎯 Burn Milestones (v3)
  if (netBurn > 0 && currentCash > 0) {
    result += "\n\n🎯 Runway Milestones\n" + "─".repeat(54);
    const milestoneMonths = [6, 12, 18, 24];
    for (const mo of milestoneMonths) {
      const cashNeeded = mo * netBurn;
      let status: string;
      if (currentCash >= cashNeeded) {
        status = "🟢";
      } else if (currentCash >= cashNeeded * 0.7) {
        status = "🟡";
      } else if (currentCash >= cashNeeded * 0.4) {
        status = "🟠";
      } else {
        status = "🔴";
      }
      result += "\n• " + mo + "-month runway:  need $" + Math.round(cashNeeded).toLocaleString() + " cash  (you have: " + fmt(currentCash) + ")  " + status;
    }
  }

  // 💡 Tip (v3)
  if (netBurn > 5) {
    result += "\n\n💡 Tip: Team costs are typically 50-70% of gross burn. If you need to extend runway fast, headcount is the biggest lever — but also the slowest to undo. Try cutting SaaS subscriptions and contractor hours first for immediate savings without firing.";
  } else if (netBurn > 0) {
    result += "\n\n💡 Tip: Burn multiple below 1.0× means you're growing revenue faster than you burn. Below 0.5× is best-in-class — investors reward capital efficiency. Track it monthly.";
  } else {
    result += "\n\n💡 Tip: Default alive is just the start. Reinvest excess cash into growth experiments (paid acquisition, content, hiring) to compound the advantage before competitors catch up.";
  }
```

- [ ] **Step 5.3: Mirror into `customFn`**

The existing `customFn` ends with `"return [r];"` at the bottom of L160. **Before** that final line, insert the minified equivalent. Since the file uses literal emojis in the source code (`'🔥 Cash Flow Health Check\n\n'`), the minified JS also uses literal emojis. The minified equivalent:

```javascript
"if(nb>0&&cc>0){r+='\\n\\n⚖️ Break-Even\\n──────────────────────────────────────────────────────';var ber=mr+nb;var gp=mr>0?((nb/mr)*100):0;if(gp>0)r+='\\n• 🟡 Need +'+fmt(nb)+'/mo revenue ('+gp.toFixed(0)+'% growth) to hit break-even';else r+='\\n• 🟡 Need +'+fmt(nb)+'/mo revenue (no revenue baseline)';r+='\\n• Or cut costs by '+fmt(nb)+'/mo (e.g., '+Math.round((nb/gb)*100)+'% of current gross burn)';r+='\\n• Break-even runway: ∞ (no burn = no runway concern)';}else if(nb<=0){r+='\\n\\n⚖️ Break-Even\\n──────────────────────────────────────────────────────';r+='\\n• 🟢 Already at break-even — revenue ≥ expenses. Focus on reinvestment.';}" +
"if(nb>0&&cc>0){r+='\\n\\n🎯 Runway Milestones\\n──────────────────────────────────────────────────────';var mms=[6,12,18,24];for(var mi=0;mi<mms.length;mi++){var cn=mms[mi]*nb;var st;if(cc>=cn)st='🟢';else if(cc>=cn*0.7)st='🟡';else if(cc>=cn*0.4)st='🟠';else st='🔴';r+='\\n• '+mms[mi]+'-month runway:  need $'+Math.round(cn).toLocaleString()+' cash  (you have: '+fmt(cc)+')  '+st;}}" +
"if(nb>5)r+='\\n\\n💡 Tip: Team costs are typically 50-70% of gross burn. If you need to extend runway fast, headcount is the biggest lever — but also the slowest to undo. Try cutting SaaS subscriptions and contractor hours first for immediate savings without firing.';else if(nb>0)r+='\\n\\n💡 Tip: Burn multiple below 1.0× means you\\'re growing revenue faster than you burn. Below 0.5× is best-in-class — investors reward capital efficiency. Track it monthly.';else r+='\\n\\n💡 Tip: Default alive is just the start. Reinvest excess cash into growth experiments (paid acquisition, content, hiring) to compound the advantage before competitors catch up.';" +
```

(Insert at the end of the existing `customFn` string concatenation, before the final `"return [r];"`.)

**Important:** do NOT change the existing literal-emoji style of the surrounding code. The new sections also use literal emojis to match.

- [ ] **Step 5.4: Update `staticExamples[0]`**

The existing `staticExamples[0]` has all-zero inputs (`monthlyRevenue=0, teamCost=0, ..., currentCash=500000`). With those inputs:
- `netBurn = 0 - 0 = 0` (gross burn is 0) — wait, actually `grossBurn = 0+0+0+0 = 0`, then `netBurn = 0 - 0 = 0` (since `monthlyRevenue=0`, but the example shows `Net Burn: $-20,000/mo`).

Let me re-check the existing static example. The example shows `Net Burn: $-20,000/mo (Gross − Revenue)` — that means `monthlyRevenue=20000` in the example. Let me re-examine:

Looking at L179: `Net Burn:      $-20,000/mo  (Gross − Revenue)` and `Annual Burn:   $-240,000/yr` — so `monthlyRevenue=20000, grossBurn=0, netBurn=-20000`. That's a negative net burn (i.e., profit).

So in the example: `netBurn = 0 - 20000 = -20000` (revenue exceeds costs). The `if (netBurn > 0)` branches in the new code won't fire. Only the `else if (netBurn <= 0)` branch fires (already at break-even).

After the existing `'\\n\\n🔄 What-If Scenarios:\\n━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\\n• ⚠️ Cannot model — ensure cash > 0 and net burn > 0.'` line, append:

```
\\n\\n⚖️ Break-Even\\n──────────────────────────────────────────────────────\\n• 🟢 Already at break-even — revenue ≥ expenses. Focus on reinvestment.\\n\\n💡 Tip: Default alive is just the start. Reinvest excess cash into growth experiments (paid acquisition, content, hiring) to compound the advantage before competitors catch up.
```

(Use literal `\n` in the TS source — same as the existing example body encoding. The runway-milestone section won't render because `netBurn <= 0` in the example, which is correct.)

- [ ] **Step 5.5: Build & verify**

```bash
pnpm build 2>&1 | tail -20
```

- [ ] **Step 5.6: Commit**

```bash
git add src/engines/burn-rate-calculator.ts
git commit -m "feat(v3): add Break-Even + Milestone + Tip sections to burn-rate-calculator"
```

---

## Task 6: market-size-estimator.ts — add ⚖️ Break-Even + 💡 Tip

**Files:**
- Modify: `src/engines/market-size-estimator.ts` — add two new sections in `calculateMarketSize()` (after the existing `// 🔄 What-If Scenarios (v3)` block, before `return [result]`), mirror into `customFn`, update `staticExamples[0]`.

**Existing state:** has 🩺 Health, 🔄 What-If, 🎯 Reality Check. **Missing ⚖️ + 💡 Tip.** Uses literal emojis (like burn-rate). 

**Known pre-existing bug to surface to user (not in scope to fix here):** L99-106 reference `penetrationRate` and `reachable` variables that are never defined in the function. This causes runtime errors in the 🩺 Health section when those branches fire. **Do not fix in this plan** — surface to user separately.

- [ ] **Step 6.1: Read the file**

Open `src/engines/market-size-estimator.ts` and locate:
- L97-106: the `// 🩺 Market Health (v3)` block (contains the pre-existing `penetrationRate` bug)
- L108-122: the `// 🔄 What-If Scenarios (v3)` block
- L124: `return [result];`
- L127-141: the `customFn` string
- L158-159: the `staticExamples`

- [ ] **Step 6.2: Insert ⚖️ + 💡 sections in `calculateMarketSize()`**

After the closing `}` of the What-If block (L122) and **before** L124's `return [result];`, insert:

```typescript
  // ⚖️ Break-Even (v3)
  if (tam > 0 && annualRevPerCustomer > 0) {
    result += "\n\n⚖️ Break-Even\n" + "─".repeat(54);
    const penetrationFor100K = (100000 / tam) * 100; // % of TAM needed for $100K/yr
    const penetrationFor1M = (1000000 / tam) * 100;
    if (penetrationFor100K < 0.1) {
      result += "\n• 🟢 To reach $100K/yr:  need " + penetrationFor100K.toFixed(3) + "% of TAM  (very achievable)";
    } else if (penetrationFor100K < 1) {
      result += "\n• 🟡 To reach $100K/yr:  need " + penetrationFor100K.toFixed(2) + "% of TAM  (achievable for focused GTM)";
    } else if (penetrationFor100K < 5) {
      result += "\n• 🟠 To reach $100K/yr:  need " + penetrationFor100K.toFixed(1) + "% of TAM  (ambitious — strong positioning needed)";
    } else {
      result += "\n• 🔴 To reach $100K/yr:  need " + penetrationFor100K.toFixed(1) + "% of TAM  (very high — consider higher pricing or larger market)";
    }
    if (penetrationFor1M < 10) {
      result += "\n• For $1M/yr:  need " + penetrationFor1M.toFixed(2) + "% of TAM  (" + Math.round(penetrationFor1M / 100 * totalCustomers).toLocaleString() + " customers)";
    } else {
      result += "\n• For $1M/yr:  need " + Math.round(penetrationFor1M).toLocaleString() + "% of TAM  (often unrealistic — likely needs VC scale)";
    }
  }

  // 🎯 Revenue Milestones (v3)
  if (tam > 0 && annualRevPerCustomer > 0 && growthRate > 0) {
    result += "\n\n🎯 Revenue Milestones\n" + "─".repeat(54);
    const targetRevenues = [100000, 1000000, 10000000];
    const labels = ["$100K ARR", "$1M ARR", "$10M ARR"];
    for (let i = 0; i < targetRevenues.length; i++) {
      const target = targetRevenues[i];
      if (tam < target) {
        result += "\n• " + labels[i] + ":  🔴 unreachable — TAM $" + (tam / 1e6).toFixed(1) + "M is smaller than target";
      } else {
        const yearsToReach = Math.log(target / tam) / Math.log(growthFactor);
        if (isFinite(yearsToReach) && yearsToReach > 0 && yearsToReach < 20) {
          result += "\n• " + labels[i] + ":  " + yearsToReach.toFixed(1) + " years  (at " + growthRate.toFixed(0) + "% market CAGR)";
        } else {
          result += "\n• " + labels[i] + ":  long horizon or zero growth";
        }
      }
    }
  }

  // 💡 Tip (v3)
  if (tam >= 1e9 && marketStage === "Growing") {
    result += "\n\n💡 Tip: Large + growing market is the ideal entry point. Don't try to serve everyone — pick a wedge (specific industry, geography, or use case) and dominate it. Expansion from a wedge is 10× easier than building a horizontal product from day one.";
  } else if (tam < 100e6 && annualRevPerCustomer < 1000) {
    result += "\n\n💡 Tip: Small market with low pricing is a tough combo. Either raise price (premium positioning) or expand the addressable market (move upmarket or downmarket). The current numbers suggest you'll struggle to reach $1M ARR.";
  } else if (marketStage === "Declining") {
    result += "\n\n💡 Tip: Declining markets punish new entrants. Unless you have a structural advantage (10× better cost structure, captive distribution), pivot to an adjacent growing segment. The market size will keep shrinking under you.";
  } else {
    result += "\n\n💡 Tip: Always cross-check bottom-up (customers × price) against top-down (TAM × market share). If the two methods diverge by 3× or more, one of your assumptions is wrong — go back to primary research.";
  }
```

- [ ] **Step 6.3: Mirror into `customFn`**

Before the final `"return [r];"` in the `customFn` string (L141), append the minified equivalent. The file uses literal emojis:

```javascript
"if(tam>0&&ar>0){r+='\\n\\n⚖️ Break-Even\\n──────────────────────────────────────────────────────';var pf100=(1e5/tam)*100;var pf1m=(1e6/tam)*100;if(pf100<0.1)r+='\\n• 🟢 To reach $100K/yr:  need '+pf100.toFixed(3)+'% of TAM  (very achievable)';else if(pf100<1)r+='\\n• 🟡 To reach $100K/yr:  need '+pf100.toFixed(2)+'% of TAM  (achievable for focused GTM)';else if(pf100<5)r+='\\n• 🟠 To reach $100K/yr:  need '+pf100.toFixed(1)+'% of TAM  (ambitious — strong positioning needed)';else r+='\\n• 🔴 To reach $100K/yr:  need '+pf100.toFixed(1)+'% of TAM  (very high — consider higher pricing or larger market)';if(pf1m<10)r+='\\n• For $1M/yr:  need '+pf1m.toFixed(2)+'% of TAM  ('+Math.round(pf1m/100*tc).toLocaleString()+' customers)';else r+='\\n• For $1M/yr:  need '+Math.round(pf1m).toLocaleString()+'% of TAM  (often unrealistic — likely needs VC scale)';}" +
"if(tam>0&&ar>0&&gr>0){r+='\\n\\n🎯 Revenue Milestones\\n──────────────────────────────────────────────────────';var trs=[1e5,1e6,1e7];var tls=['$100K ARR','$1M ARR','$10M ARR'];for(var ri=0;ri<trs.length;ri++){var tgt=trs[ri];if(tam<tgt)r+='\\n• '+tls[ri]+':  🔴 unreachable — TAM $'+(tam/1e6).toFixed(1)+'M is smaller than target';else{var y2=Math.log(tgt/tam)/Math.log(gf);if(isFinite(y2)&&y2>0&&y2<20)r+='\\n• '+tls[ri]+':  '+y2.toFixed(1)+' years  (at '+gr.toFixed(0)+'% market CAGR)';else r+='\\n• '+tls[ri]+':  long horizon or zero growth';}}}" +
"if(tam>=1e9&&ms==='Growing')r+='\\n\\n💡 Tip: Large + growing market is the ideal entry point. Don\\'t try to serve everyone — pick a wedge (specific industry, geography, or use case) and dominate it. Expansion from a wedge is 10× easier than building a horizontal product from day one.';else if(tam<1e8&&ar<1000)r+='\\n\\n💡 Tip: Small market with low pricing is a tough combo. Either raise price (premium positioning) or expand the addressable market (move upmarket or downmarket). The current numbers suggest you\\'ll struggle to reach $1M ARR.';else if(ms==='Declining')r+='\\n\\n💡 Tip: Declining markets punish new entrants. Unless you have a structural advantage (10× better cost structure, captive distribution), pivot to an adjacent growing segment. The market size will keep shrinking under you.';else r+='\\n\\n💡 Tip: Always cross-check bottom-up (customers × price) against top-down (TAM × market share). If the two methods diverge by 3× or more, one of your assumptions is wrong — go back to primary research.';" +
```

(Insert at the end of the existing `customFn` string concatenation, before the final `"return [r];"`.)

- [ ] **Step 6.4: Update `staticExamples[0]`**

The existing `staticExamples[0]` has all-zero inputs (`totalAddressableCustomers=0, annualRevenuePerCustomer=0, marketGrowthRate=0, ...`). The new sections will short-circuit on the `tam > 0` check in the ⚖️ block (since `tam = 0 * 0 = 0`). The 💡 Tip block does check `marketStage === "Growing"` (default in the example) but `tam >= 1e9` is false (0 < 1e9), and the other conditions also fail (0 < 1e8 is true but 0 < 1000 is also true... wait, `0 < 1000` is true AND `0 < 1e8` is true, so the small-market-low-pricing branch fires).

So in the example, only the small-market-low-pricing tip renders. After the existing example content, append:

```
\\n\\n💡 Tip: Small market with low pricing is a tough combo. Either raise price (premium positioning) or expand the addressable market (move upmarket or downmarket). The current numbers suggest you'll struggle to reach $1M ARR.
```

(Use literal `\n` in the TS source. Place before the array's closing `'`.)

- [ ] **Step 6.5: Build & verify**

```bash
pnpm build 2>&1 | tail -20
```

- [ ] **Step 6.6: Surface the pre-existing 🩺 bug to the user**

Open a separate conversation / message at end of execution: the `penetrationRate` and `reachable` variables in the 🩺 Health block (L99-106) are undefined. When `totalCustomers > 0` (which happens in real usage), this throws. **Not fixed in this plan** — needs a follow-up task to define these vars (probably as `penetrationRate = 2` and `reachable = totalCustomers * 0.02` or similar — confirm with user what the right values are).

- [ ] **Step 6.7: Commit**

```bash
git add src/engines/market-size-estimator.ts
git commit -m "feat(v3): add Break-Even + Tip sections to market-size-estimator (note: pre-existing 🩺 bug surfaced separately)"
```

---

## Task 7: unit-economics-calculator.ts — add ⚖️ Break-Even + 💡 Tip

**Files:**
- Modify: `src/engines/unit-economics-calculator.ts` — add two new sections in `calculateUnitEconomics()` (after the existing `// 🔄 What-If Scenarios (v3)` block, before `return out`), mirror into `customFn`, update `staticExamples[0]`.

**Existing state:** has 🩺 Health, 🔄 What-If. Has 🎯 Optimization Levers + 🏆 Lever Impact Ranking (covers milestone-like content). **Missing ⚖️ + 💡 Tip.** Uses lowercase `\u` escapes (`📦`) — preserve this style for consistency with the rest of the file. **This file uses `\ud83d` lowercase style, different from the `\uD83D` uppercase in other engines.** Do NOT normalize.

- [ ] **Step 7.1: Read the file**

Open `src/engines/unit-economics-calculator.ts` and locate:
- L187-211: the `// 🔄 What-If Scenarios (v3)` block
- L213: `return out;`
- L216-240: the `customFn` string (note: uses lowercase `📦` etc., not `📦`)
- L257-258: the `staticExamples`

- [ ] **Step 7.2: Insert ⚖️ + 💡 sections in `calculateUnitEconomics()`**

After the closing of the What-If block (L211) and **before** L213's `return out;`, insert:

```typescript
  // ⚖️ Break-Even (v3)
  if (netContribution > 0) {
    out.push("⚖️ Break-Even");
    out.push("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━");
    if (cac > 0 && isFinite(paybackMonths)) {
      out.push("• Break-even customers/mo needed to amortize $1 of CAC:");
      out.push("  1 customer covers $" + fmt(netContribution) + "/mo  →  need " + Math.round(cac / netContribution) + " customers acquired per $1 CAC = total $1 of CAC to reach payback");
      out.push("  Current CAC $" + fmt(cac) + " needs " + Math.round(cac / netContribution) + " customers contributing net profit for " + paybackMonths.toFixed(1) + " months to break even");
    }
    out.push("• Break-even monthly volume:  " + (costServe > 0 && netContribution > 0 ? Math.ceil(costServe / (totalRev - costServe)) : 0) + " customer-months of net-positive contribution to cover variable costs (informational)");
    if (cac === 0) {
      out.push("• No CAC — every customer is profitable from month 1. Pure growth mode.");
    } else {
      out.push("• Once " + Math.round(cac / netContribution) + " customers are acquired, monthly net profit is positive thereafter (assuming constant churn).");
    }
    out.push("");
  }

  // 💡 Tip (v3)
  if (netContribution <= 0) {
    out.push("💡 Tip: Negative net contribution means you lose money on every customer served. Fix this before scaling — either raise price, cut cost-to-serve (hosting, support, payment processing), or both. Scaling a loss-making unit is a guaranteed death spiral.");
  } else if (isFinite(paybackMonths) && paybackMonths > 12) {
    out.push("💡 Tip: CAC payback over 12 months is the #1 red flag for SaaS investors. Two ways out: (1) raise prices 20-30% (most founders underprice), or (2) cut CAC by improving conversion (better landing page, more qualified leads). Either compounds — don't pick both at once.");
  } else if (monthlyChurn > 0.05) {
    out.push("💡 Tip: 5%+ monthly churn is the silent killer. The math: cutting churn from 5% to 3% roughly doubles LTV. That's usually worth more than a new pricing tier. Talk to churned customers first — the answer is almost always in their exit interviews.");
  } else {
    out.push("💡 Tip: Solid unit economics. The next bottleneck is usually distribution (filling the funnel), not the per-customer math. If you're hitting LTV:CAC ≥ 3:1 with payback < 12mo, consider raising prices — the market can probably bear more than you're charging.");
  }
```

- [ ] **Step 7.3: Mirror into `customFn`**

Before the final `"return [r];"` in the `customFn` string (L240), append the minified equivalent. **Use the same lowercase `📦` style as the rest of the file:**

```javascript
"if(nc>0){r+='\\n\\u2696\\uFE0F Break-Even\\n\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501';if(cac>0&&isFinite(pm)){r+='\\n\\u2022 Break-even customers/mo needed to amortize CAC:\\n  Current CAC $'+fmt(cac)+' needs '+Math.round(cac/nc)+' customers contributing net profit for '+pm.toFixed(1)+' months to break even';}if(cac===0)r+='\\n\\u2022 No CAC \\u2014 every customer is profitable from month 1. Pure growth mode.';else r+='\\n\\u2022 Once '+Math.round(cac/nc)+' customers are acquired, monthly net profit is positive thereafter (assuming constant churn).';}" +
"if(nc<=0)r+='\\n\\uD83D\\uDCA1 Tip: Negative net contribution means you lose money on every customer served. Fix this before scaling \\u2014 either raise price, cut cost-to-serve (hosting, support, payment processing), or both. Scaling a loss-making unit is a guaranteed death spiral.';else if(isFinite(pm)&&pm>12)r+='\\n\\uD83D\\uDCA1 Tip: CAC payback over 12 months is the #1 red flag for SaaS investors. Two ways out: (1) raise prices 20-30% (most founders underprice), or (2) cut CAC by improving conversion (better landing page, more qualified leads). Either compounds \\u2014 don\\'t pick both at once.';else if(mc>0.05)r+='\\n\\uD83D\\uDCA1 Tip: 5%+ monthly churn is the silent killer. The math: cutting churn from 5% to 3% roughly doubles LTV. That\\'s usually worth more than a new pricing tier. Talk to churned customers first \\u2014 the answer is almost always in their exit interviews.';else r+='\\n\\uD83D\\uDCA1 Tip: Solid unit economics. The next bottleneck is usually distribution (filling the funnel), not the per-customer math. If you\\'re hitting LTV:CAC \\u2265 3:1 with payback < 12mo, consider raising prices \\u2014 the market can probably bear more than you\\'re charging.';" +
```

(Insert at the end of the existing `customFn` string concatenation, before the final `"return [r];"`.)

- [ ] **Step 7.4: Update `staticExamples[0]`**

The existing `staticExamples[0]` has all-positive example inputs (`averageRevenuePerCustomer=100, expansionRevenuePerCustomer=30, costToServePerCustomer=30, customerAcquisitionCost=300, monthlyChurnRate=5, retentionMonths=0`). With those:
- `netContribution = 100+30-30 = 100`
- `cac = 300`, `paybackMonths = 3.0`
- `monthlyChurn = 0.05`

So the `else if (monthlyChurn > 0.05)` branch **doesn't** fire (0.05 is not > 0.05), and `paybackMonths <= 12` is true. Falls through to the `else` branch: "Solid unit economics" tip.

After the existing example content, append:

```
\\n\\n⚖️ Break-Even\\n━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\\n• Break-even customers/mo needed to amortize CAC:\\n  Current CAC $300 needs 3 customers contributing net profit for 3.0 months to break even\\n• Once 3 customers are acquired, monthly net profit is positive thereafter (assuming constant churn).\\n\\n💡 Tip: Solid unit economics. The next bottleneck is usually distribution (filling the funnel), not the per-customer math. If you're hitting LTV:CAC ≥ 3:1 with payback < 12mo, consider raising prices — the market can probably bear more than you're charging.
```

(Use literal `\n` in the TS source. Place before the array's closing `'`.)

- [ ] **Step 7.5: Build & verify**

```bash
pnpm build 2>&1 | tail -20
```

- [ ] **Step 7.6: Commit**

```bash
git add src/engines/unit-economics-calculator.ts
git commit -m "feat(v3): add Break-Even + Tip sections to unit-economics-calculator"
```

---

## Task 8: saas-valuation-calculator.ts — add ⚖️ Break-Even + 🎯 Milestone

**Files:**
- Modify: `src/engines/saas-valuation-calculator.ts` — add two new sections in `calculateValuation()` (after the existing `// 🔄 What-If Scenarios (v3)` block, before `results.push(mainResult)`), mirror into `customFn`, update `staticExamples[0]`.

**Existing state:** has 🩺 Health, 🔄 What-If, 💡 Tip. **Missing ⚖️🎯.** Uses `\u` escapes.

- [ ] **Step 8.1: Read the file**

Open `src/engines/saas-valuation-calculator.ts` and locate:
- L85-102: the `// 🔄 What-If Scenarios (v3)` block
- L104: `results.push(mainResult);`
- L126-172: the `customFn` string
- L192-198: the `staticExamples`

- [ ] **Step 8.2: Insert ⚖️ + 🎯 sections in `calculateValuation()`**

After the closing `}` of the What-If else-branch (L102) and **before** L104's `results.push(mainResult);`, insert:

```typescript
  // ⚖️ Break-Even Multiple (v3)
  if (annualRevenue > 0) {
    mainResult += '\\n\\n⚖️ Break-Even Multiple\\n━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━';
    if (baseMultiple < 3) {
      mainResult += '\\n• 🔴 Current multiple ' + baseMultiple.toFixed(1) + 'x is below 3x SaaS floor — investors won\\'t bite.';
      mainResult += '\\n• To reach 3x floor:  need growth ' + Math.max(10, growthRate).toFixed(0) + '%+  AND  margin ' + Math.max(20, profitMargin).toFixed(0) + '%+';
    } else if (baseMultiple < 5) {
      mainResult += '\\n• 🟡 Current multiple ' + baseMultiple.toFixed(1) + 'x is at SaaS floor. Below market median (5-8x).';
      mainResult += '\\n• To reach 5x (median):  grow 30%+ YoY, OR push margin above 20%';
    } else if (baseMultiple < 10) {
      mainResult += '\\n• 🟢 Current multiple ' + baseMultiple.toFixed(1) + 'x is at market median. Strong position.';
      mainResult += '\\n• To reach 10x+ (premium):  sustain 50%+ growth OR reach profitability';
    } else {
      mainResult += '\\n• 🟢 Premium multiple ' + baseMultiple.toFixed(1) + 'x. Maintain growth and margin to justify.';
    }
  }

  // 🎯 Valuation Milestones (v3)
  if (annualRevenue > 0) {
    mainResult += '\\n\\n🎯 Valuation Milestones\\n━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━';
    const milestones = [
      { arr: 1e6, label: '$1M ARR' },
      { arr: 1e7, label: '$10M ARR' },
      { arr: 1e8, label: '$100M ARR' },
    ];
    for (const m of milestones) {
      if (annualRevenue >= m.arr) {
        mainResult += '\\n• ' + m.label + ':  ✅ reached  (current: $' + (annualRevenue / 1e6).toFixed(1) + 'M)';
      } else if (growthRate > 0) {
        const yearsToReach = Math.log(m.arr / annualRevenue) / Math.log(1 + growthRate / 100);
        if (isFinite(yearsToReach) && yearsToReach < 20) {
          mainResult += '\\n• ' + m.label + ':  ' + yearsToReach.toFixed(1) + ' years  (at ' + growthRate.toFixed(0) + '% YoY growth)';
        } else {
          mainResult += '\\n• ' + m.label + ':  unrealistic at current growth — accelerate or accept smaller scale';
        }
      } else {
        mainResult += '\\n• ' + m.label + ':  zero growth — not reachable without acceleration';
      }
    }
  }
```

- [ ] **Step 8.3: Mirror into `customFn`**

Before the final `"return results;"` in the `customFn` string (after L171), append the minified equivalent:

```javascript
"if(ar2>0){mr4+='\\n\\n\\u2696\\uFE0F Break-Even Multiple\\n\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501';if(bm<3){mr4+='\\n\\u2022 \\uD83D\\uDD34 Current multiple '+bm.toFixed(1)+'x is below 3x SaaS floor \\u2014 investors won\\'t bite.\\n\\u2022 To reach 3x floor:  need growth '+Math.max(10,gr2).toFixed(0)+'%+  AND  margin '+Math.max(20,pm).toFixed(0)+'%+';}else if(bm<5){mr4+='\\n\\u2022 \\uD83D\\uDFE1 Current multiple '+bm.toFixed(1)+'x is at SaaS floor. Below market median (5-8x).\\n\\u2022 To reach 5x (median):  grow 30%+ YoY, OR push margin above 20%';}else if(bm<10){mr4+='\\n\\u2022 \\uD83D\\uDFE2 Current multiple '+bm.toFixed(1)+'x is at market median. Strong position.\\n\\u2022 To reach 10x+ (premium):  sustain 50%+ growth OR reach profitability';}else mr4+='\\n\\u2022 \\uD83D\\uDFE2 Premium multiple '+bm.toFixed(1)+'x. Maintain growth and margin to justify.';}" +
"if(ar2>0){mr4+='\\n\\n\\uD83C\\uDFAF Valuation Milestones\\n\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501\\u2501';var vms=[{a:1e6,l:'$1M ARR'},{a:1e7,l:'$10M ARR'},{a:1e8,l:'$100M ARR'}];for(var vi=0;vi<vms.length;vi++){var vm=vms[vi];if(ar2>=vm.a)mr4+='\\n\\u2022 '+vm.l+':  \\u2705 reached  (current: $'+(ar2/1e6).toFixed(1)+'M)';else if(gr2>0){var ytr=Math.log(vm.a/ar2)/Math.log(1+gr2/100);if(isFinite(ytr)&&ytr<20)mr4+='\\n\\u2022 '+vm.l+':  '+ytr.toFixed(1)+' years  (at '+gr2.toFixed(0)+'% YoY growth)';else mr4+='\\n\\u2022 '+vm.l+':  unrealistic at current growth \\u2014 accelerate or accept smaller scale';}else mr4+='\\n\\u2022 '+vm.l+':  zero growth \\u2014 not reachable without acceleration';}}" +
```

- [ ] **Step 8.4: Update `staticExamples[0]`**

The existing `staticExamples[0]` has all-zero inputs (`annualRevenue=0, growthRate=100, profitMargin=0`). With those:
- `baseMultiple` is 10 (because `growthRate >= 100` triggers that branch)
- `annualRevenue = 0`, so the new sections short-circuit (the `if (annualRevenue > 0)` check fails in both new sections)

So nothing additional renders in the example. No update needed for `staticExamples[0]`. (The other staticExamples `1-4` use real revenue values; those also don't currently include the v3 sections, but for now the page initial render uses `staticExamples[0]`. After browser interaction, `customFn` runs and the new sections will appear.)

**But verify:** the v3 sections in `customFn` are gated on `ar2 > 0`, so they only fire when the user enters non-zero revenue. The static example is for zero revenue (placeholder), so no new content there is correct.

- [ ] **Step 8.5: Build & verify**

```bash
pnpm build 2>&1 | tail -20
```

Expected: build succeeds. The v3 sections will appear in the browser when the user enters a non-zero ARR.

- [ ] **Step 8.6: Commit**

```bash
git add src/engines/saas-valuation-calculator.ts
git commit -m "feat(v3): add Break-Even + Milestone sections to saas-valuation-calculator"
```

---

## Task 9: Final build + coverage verification

- [ ] **Step 9.1: Re-run the full build**

```bash
pnpm build 2>&1 | tail -30
```

Expected: build succeeds. All 32 engine pages regenerate.

- [ ] **Step 9.2: Verify v3 marker coverage**

```bash
grep -l "⚖️\|\\\\u2696" src/engines/*.ts | wc -l   # expect 32 (all 32 engines have ⚖️ OR justified exemption per the AI Cost variant)
grep -l "🎯\|\\\\uD83C\\\\uDFAF" src/engines/*.ts | wc -l   # expect 32
```

Note: the AI cost engines don't have ⚖️🎯 in code (they're exempt per the tiered standard). The grep above will return ~24-27 (the 7 changed + the previously-19-with-both). The new CLAUDE.md documents why the AI cost engines don't have these — that's the right answer, not a bug.

- [ ] **Step 9.3: Update CLAUDE.md task line**

Re-open `CLAUDE.md` and update the v3 status paragraph (added in Task 1) if any counts shifted during execution (e.g., if a particular engine's example needed additional content). The plan assumes 7 engines get full Business v3, and the 8 AI cost engines stay on AI Cost v3 — total 32/32 in the new tiered definition.

- [ ] **Step 9.4: Final commit (if any doc changes from Step 9.3)**

```bash
git add CLAUDE.md
git commit -m "docs(CLAUDE.md): reflect post-execution v3 status (32/32 in tiered definition)"
```

---

## Self-Review

**Spec coverage:**
- [x] CLAUDE.md corrected (Task 1) — fixes the "32/32 ✅" lie
- [x] Tiered v3 standard defined (Task 1.2)
- [x] All 7 engines get ⚖️ Break-Even (Tasks 2, 3, 4, 5, 6, 7, 8)
- [x] All 7 engines get 🎯 Milestone (Tasks 2, 3, 4, 5, 6, 8 — for `unit-economics` the existing "🎯 Optimization Levers" section already serves as the milestone projection; Task 7 only adds ⚖️ + 💡 since 🎯 is present)
- [x] 💡 Tip restored where missing (Tasks 5, 6, 7 — burn-rate, market-size, unit-economics)
- [x] AI cost 8 engines documented as exempt (Task 1.2)
- [x] Pre-existing 🩺 bug in `market-size-estimator.ts` surfaced (Task 6.6)

**Placeholder scan:** No "TODO", "TBD", "fill in details" in the plan body. All code blocks are concrete and ready to paste.

**Type consistency:** The `customFn` minified JS uses the same variable names as the existing `customFn` (e.g., `cac2`, `pbm`, `arc`, `gpc` in CAC; `ltv`, `cacV`, `lcr` in LTV; `cs`, `cl`, `nc`, `mlc` in churn; `mr`, `nb`, `gb`, `cc` in burn-rate; `tc`, `ar`, `ms`, `tam` in market-size; `rc`, `er`, `cs`, `cac`, `cr`, `nc`, `pm`, `ltv` in unit-economics; `ar2`, `gr2`, `pm`, `bm` in valuation). These are the same names used in the existing `customFn` code, so the new code appended to each engine is consistent with its file.

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-06-22-close-v3-gap-7-business-calculators.md`.

**Two execution options:**

1. **Subagent-Driven (recommended)** — dispatch a fresh subagent per task, review between tasks, fast iteration
2. **Inline Execution** — execute tasks in this session using `executing-plans`, batch execution with checkpoints

Which approach?
