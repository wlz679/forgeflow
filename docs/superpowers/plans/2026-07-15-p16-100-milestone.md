# P16 100-Milestone Batch Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Reach 100 calculators + close all known debt BEFORE entering event-driven maintenance mode. Ship 2 new e-commerce engines (M: 6→8) + extend defense-in-depth layer to 42 pre-P5 engines + bundle 4 P15 polish items.

**Architecture:** Reuse P14-Followup + P15 defense-in-depth pattern (`clampNonNegative` programmatic guard + `var cnn=...` customFn inlining). HTML5 layer is ALREADY in place for all engines via `[slug].astro:940` template (verified via dist grep on 4 BIZ_CONFIG engines). New engines build with full pattern from start; old-pattern sweep only needs programmatic + customFn layers (HTML5 already present). Per-task atomic commits; per-batch sample-verify before sweep.

**Tech Stack:** Astro 4.16.19 + TypeScript 5.6 strict + `node:test` + `node:assert/strict` + custom engine runtime + satori OG image generation.

## Global Constraints

The spec's project-wide requirements (from `docs/superpowers/specs/2026-07-15-p16-100-milestone-design.md`):

- **98 → 100 engines** by ship. E-Commerce / Marketing is the chosen vertical for the 2 new engines (zero existing coverage; solopreneur hot search topic).
- **Engine categories** — 15 categories with letters B/C/D/E/I/R/M/O/S/P/K/L (P series 6-14) + AI Cost data-driven. M (Marketing) goes 6→8 with coupon-attribution + cart-abandonment.
- **42 old-pattern engines** across 7 categories (cost 4 · freelance 3 · hiring-team 6 · investment 5 · saas 5 · real-estate 6 · valuation 13) — use `[slug].astro:940` Astro template (NOT a separate BIZ_CONFIG form renderer; BIZ_CONFIG only controls visual chart-icon families at line 1469-1472).
- **HTML5 layer already present** for all engines at `[slug].astro:940` (verified via dist grep showing `step="any"` on 4 sample BIZ_CONFIG engines: meeting-cost, ltv, compound-interest, mortgage). Tasks 3-5 only apply programmatic + customFn layers.
- **8 AI Cost engines** in `src/engines/ai-cost/` — ALREADY DONE in P15 Task 2 (4 engines swept, other 4 were P-series or already-clamped). **OUT OF SCOPE** for P16.
- **New-pattern engines** (P6-P14, 48 engines) — ALREADY DONE in P14-Followup. **OUT OF SCOPE** for P16.
- **Defense-in-depth pattern** (per engine): HTML5 `step="any" min="0"` (already in template) + programmatic `clampNonNegative(parseFloat(x) || 0)` in `calculate()`/`generate()` + `var cnn=function(x){return Math.max(0,x)};` inline at top of customFn + wrap negative-prone input-parses with `cnn(...)`.
- **Pre-existing pnpm check flake**: use `SKIP_PRECOMMIT_CHECK=1` for commits (only when intentional).
- **Dual-push**: `git push origin HEAD` (gitee) then `git push github HEAD` (or `SKIP_PUSH_FETCH=1` if local in sync with origin).
- **NPS special case** (P9-5 only): `min="-100"` only for `input.name === 'nps'`. Already in template.
- **Helper**: `clampNonNegative(x: number): number` in `src/core/engines/helpers.ts:67`. NaN guard: returns 0 for NaN, Math.max(0,x) otherwise.
- **Tooling available** (already shipped in P15): `scripts/codegen-examples.mjs --check`, `scripts/codegen-customfn.mjs --check`, `scripts/smoke-html5.mjs --strict`, `tests/scripts/verify-customfn.mjs`, `tests/scripts/test-customFn.mjs`.

---

## File Structure

| File | Status | Purpose |
|---|---|---|
| `src/engines/marketing/coupon-attribution-calculator.ts` | CREATE | New engine 99 (5 inputs, 6 outputs, true_roi health band) |
| `tests/coupon-attribution-calculator.test.ts` | CREATE | Per-engine tests (5 min) |
| `src/engines/marketing/cart-abandonment-cost-calculator.ts` | CREATE | New engine 100 (6 inputs, 8 outputs, recovery_roi health band) |
| `tests/cart-abandonment-cost-calculator.test.ts` | CREATE | Per-engine tests (6 min) |
| `src/engines/marketing/index.ts` | MODIFY | Add 2 imports/exports |
| `src/engines/cost/{employee-cost,meeting-cost,productivity-score,remote-vs-office}-calculator.ts` (×4) | MODIFY | Add clamp + cnn |
| `tests/cost/{...}-calculator.test.ts` (×4) | MODIFY | +1 defensive test each |
| `src/engines/freelance/{affiliate-income,freelance-rate,hourly-vs-fixed}-calculator.ts` (×3) | MODIFY | Add clamp + cnn |
| `tests/freelance/{...}-calculator.test.ts` (×3) | MODIFY | +1 defensive test each |
| `src/engines/hiring-team/{attrition-cost,comp-banding,equity-refresh,fully-loaded-employee-cost,productivity-ramp-curve,time-to-productivity}-calculator.ts` (×6) | MODIFY | Add clamp + cnn (P11 — verify pre-existing first) |
| `tests/hiring-team/{...}-calculator.test.ts` (×6) | MODIFY | +1 defensive test each |
| `src/engines/investment/{compound-interest,equity-dilution,freelance-tax,sponsorship-rate,time-value}-calculator.ts` (×5) | MODIFY | Add clamp + cnn |
| `tests/investment/{...}-calculator.test.ts` (×5) | MODIFY | +1 defensive test each |
| `src/engines/real-estate/{brrrr,cap-rate,dscr,mortgage,rental-yield,rent-vs-buy}-calculator.ts` (×6) | MODIFY | Add clamp + cnn (P5 — verify pre-existing first) |
| `tests/real-estate/{...}-calculator.test.ts` (×6) | MODIFY | +1 defensive test each |
| `src/engines/saas/{burn-rate,churn-rate,market-size-estimator,mrr,revenue-projector}-calculator.ts` (×5) | MODIFY | Add clamp + cnn |
| `tests/saas/{...}-calculator.test.ts` (×5) | MODIFY | +1 defensive test each |
| `src/engines/valuation/{arr-multiple-valuation,break-even,burn-multiple-rule-of-40,cac,course-pricing,email-list-revenue,ltv,project-profitability,saas-pricing-planner,saas-valuation,safe-convertible-note,stripe-fee,unit-economics}-calculator.ts` (×13) | MODIFY | Add clamp + cnn |
| `tests/valuation/{...}-calculator.test.ts` (×13) | MODIFY | +1 defensive test each |
| `tests/baselayout-clerk-script.test.ts` | MODIFY | Add skip-marker for missing env (P15-5 polish) |
| `tests/{acv,funnel-step,article-helpfulness}-calculator.test.ts` (3) | MODIFY | Stronger assertions (P15-7 polish) |
| `src/core/engines/helpers.ts:72` + `tests/helpers.test.ts` | MODIFY | Remove no-op `as number` cast + rename Test 5 (P15-9 polish) |
| `src/engines/customer-support/first-response-time-calculator.ts` | MODIFY | Extract `LIFT_PERCENT = 5` constant (P15-10 polish) |
| `memory/p16-100-milestone-shipped.md` | CREATE | Ship memory |
| `memory/MEMORY.md` (in-repo + Claude-side) | MODIFY | Add P16 pointer |

**Total**: 100 engines after P16 (98 + 2 new). 42 old-pattern engines get clamp + cnn. 4 P15 polish items closed. ~95 files touched, ~1480 LOC.

---

### Task 1: New engine — `coupon-attribution-calculator` (engine 99)

**Files:**
- Create: `src/engines/marketing/coupon-attribution-calculator.ts`
- Create: `tests/coupon-attribution-calculator.test.ts`
- Modify: `src/engines/marketing/index.ts` (add import + export)

**Interfaces:**
- Consumes: `clampNonNegative` from `../../core/engines/helpers` (existing); `ToolEngine` type from `../../core/engines/types`; `registerEngine` from `../../core/engines/registry`
- Produces: `coupon-attribution-calculator` engine (slug) with 5 inputs + 6 outputs + 🟢≥100% / 🟡0-100% / 🔴<0% health band on `true_roi`

**Spec details** (from `docs/superpowers/specs/2026-07-15-p16-100-milestone-design.md` §4.1):

| Input | Type | Default | Range |
|---|---|---|---|
| `coupon_value` | $ | 20 | 0-10000 |
| `redemption_rate` | % | 10 | 0-100 |
| `avg_order_value` | $ | 80 | 1-10000 |
| `baseline_revenue` | $ | 50000 | 0-10000000 |
| `cannibalization_pct` | % | 30 | 0-100 |

**Outputs (6):** `total_coupon_revenue`, `incremental_revenue`, `cannibalization_loss`, `coupon_cost`, `net_revenue_gain`, `true_roi`

**Canonical example** (verify math):
- Inputs: 20, 10%, 80, 50000, 30%
- total = (50000 × 0.10) × 80 = $400,000
- incremental = 400,000 × 0.7 = $280,000
- coupon_cost = 50000 × 0.10 × 20 = $100,000
- net = 280,000 - 100,000 = $180,000
- ROI = 180,000 / 100,000 = **180% → 🟢 Good**

**Health band helper text**: *"Cannibalization: not running A/B tests? Use 30% (industry avg). Shopify/Klaviyo standard definition."*

**v3 standard**: Business variant (6+ emoji sections: 🩺 Health · 🔄 What-If · ⚖️ Break-Even · 🎯 Milestone · 💡 Tip + 1 more).

- [ ] **Step 1: Write failing tests (canonical + 3 health bands + defensive clamp + edge case)**

Create `tests/coupon-attribution-calculator.test.ts`:

```ts
import { test } from 'node:test';
import assert from 'node:assert/strict';
import {
  calcTrueROI,
  calcHealthBand,
} from '../src/engines/marketing/coupon-attribution-calculator';

// Canonical: 20, 10%, 80, 50000, 30% → true_roi = 180%
test('coupon-attribution: canonical inputs → 180% ROI → 🟢 Good', () => {
  const couponValue = 20;
  const redemptionPct = 10;
  const aov = 80;
  const baseline = 50000;
  const cannibalization = 30;
  const total = (baseline * (redemptionPct / 100)) * aov;
  const couponCost = baseline * (redemptionPct / 100) * couponValue;
  const incremental = total * (1 - cannibalization / 100);
  const net = incremental - couponCost;
  const roi = net / couponCost;
  assert.ok(Math.abs(roi - 1.80) < 0.01, `expected 1.80, got ${roi.toFixed(2)}`);
  assert.equal(calcHealthBand(roi), 'good');
});

// Health bands: 3 boundary tests
test('coupon-attribution: ROI ≥ 100% → good', () => {
  assert.equal(calcHealthBand(1.5), 'good');
  assert.equal(calcHealthBand(1.0), 'good');
});

test('coupon-attribution: ROI 0-100% → warning', () => {
  assert.equal(calcHealthBand(0.5), 'warning');
  assert.equal(calcHealthBand(0.0), 'warning');
});

test('coupon-attribution: ROI < 0% → critical', () => {
  assert.equal(calcHealthBand(-0.5), 'critical');
});

// Defensive clamp: negative input clamps to 0
test('coupon-attribution: negative coupon_value clamps to 0 (defensive layer 2)', () => {
  // clampNonNegative(-20) → 0; calcTrueROI(0, ...) → 0 (avoid NaN/negative ratio)
  const roi = calcTrueROI(0, 10, 80, 50000, 30);
  assert.equal(roi, 0);
  assert.ok(roi >= 0);
  assert.ok(!isNaN(roi));
});

// Edge case: all zeros → ROI undefined → 0 or NaN (guard)
test('coupon-attribution: all-zero inputs → guard handles gracefully', () => {
  const roi = calcTrueROI(0, 0, 0, 0, 0);
  assert.ok(roi >= 0 || isNaN(roi)); // Accept either 0 or NaN; never Infinity or negative
});
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `node --import tsx tests/coupon-attribution-calculator.test.ts`
Expected: FAIL with "Cannot find module" (calcTrueROI, calcHealthBand not exported yet).

- [ ] **Step 3: Implement engine with full defense-in-depth pattern**

Create `src/engines/marketing/coupon-attribution-calculator.ts` following the roas-calculator.ts pattern (already read). Include:

1. Imports: `ToolEngine`, `registerEngine`, `clampNonNegative`
2. `HEALTH_BANDS` const (exported for tests)
3. `calcTrueROI(...)` math helper (exported for tests) — accept clamped inputs, return ROI or 0
4. `calcHealthBand(roi)` helper (exported for tests) — return 'good'/'warning'/'critical'
5. `calculate(inputs)` — parse inputs with `clampNonNegative`, compute outputs, build v3 standard 6-section output
6. `customFn` (minified) — prepend `var cnn=function(x){return Math.max(0,x)};` then mirror calculate() with `cnn(...)` wrapping each parseFloat
7. `engine` object with slug, title, description, inputs (5), clientConfig (custom + customFn), calculate, generate: calculate, staticExamples, faq (4), howToUse (4-5), keywords, tags, sources
8. `registerEngine(engine)` call

Reference: copy roas-calculator.ts structure exactly; replace math helpers with coupon-specific; replace emoji sections to fit coupon narrative.

**CustomFn snippet** (the critical P14-Followup pattern):
```js
const customFn =
  "var cnn=function(x){return Math.max(0,x)};" +
  "var cv=cnn(parseFloat(inputs.couponValue)||0);" +
  "var rr=cnn(parseFloat(inputs.redemptionRate)||0);" +
  "var aov=cnn(parseFloat(inputs.avgOrderValue)||0);" +
  "var br=cnn(parseFloat(inputs.baselineRevenue)||0);" +
  "var cp=cnn(parseFloat(inputs.cannibalizationPct)||0);" +
  // ... rest mirrors calculate() ...
```

- [ ] **Step 4: Verify tests pass**

Run: `node --import tsx tests/coupon-attribution-calculator.test.ts`
Expected: 5/5 pass.

- [ ] **Step 5: Verify customFn parses**

Run: `node tests/scripts/verify-customfn.mjs coupon-attribution-calculator`
Expected: `OK (N chars) [style-name]`

- [ ] **Step 6: Regenerate staticExamples[0] from calculate()**

Run: `node scripts/codegen-examples.mjs`
Expected: staticExamples[0] updated to match calculate() output exactly.

- [ ] **Step 7: Add import + export to marketing/index.ts**

Modify `src/engines/marketing/index.ts`:
```ts
// Add at end:
export * from './coupon-attribution-calculator';
```

Add header comment:
```ts
// P16-1 coupon-attribution-calculator.ts -- added in P16-1
```

- [ ] **Step 8: Verify build still succeeds**

Run: `pnpm exec astro build 2>&1 | tail -5`
Expected: 310 static pages built (309 + 1 new), no errors.

- [ ] **Step 9: Verify HTML5 layer on new page**

Run: `node scripts/smoke-html5.mjs --strict`
Expected: exit 0, no new missing step="any".

- [ ] **Step 10: Commit**

```bash
git add src/engines/marketing/coupon-attribution-calculator.ts tests/coupon-attribution-calculator.test.ts src/engines/marketing/index.ts
git commit -m "feat(p16-1): coupon-attribution-calculator — M: 6→7 (engine 99)"
```

---

### Task 2: New engine — `cart-abandonment-cost-calculator` (engine 100)

**Files:**
- Create: `src/engines/marketing/cart-abandonment-cost-calculator.ts`
- Create: `tests/cart-abandonment-cost-calculator.test.ts`
- Modify: `src/engines/marketing/index.ts` (add import + export)

**Interfaces:**
- Consumes: same as Task 1
- Produces: `cart-abandonment-cost-calculator` engine with 6 inputs + 8 outputs + 🟢≥300% / 🟡100-300% / 🔴<100% health band on `recovery_roi`

**Spec details** (from spec §4.1):

| Input | Type | Default | Range |
|---|---|---|---|
| `monthly_traffic` | int | 50000 | 0-10000000 |
| `cart_add_rate` | % | 20 | 0-100 |
| `cart_abandonment_rate` | % | 70 | 0-99 |
| `avg_order_value` | $ | 80 | 1-10000 |
| `recovery_rate` | % | 8 | 0-100 |
| `recovery_cost_per_send` | $ | 0.5 | 0-50 |

**Outputs (8):** `cart_creations`, `completed_orders`, `abandoned_carts`, `lost_revenue`, `recoverable_revenue`, `recovery_cost`, `recovery_net_gain`, `recovery_roi`

**Canonical example** (verify math):
- Inputs: 50000, 20, 70, 80, 8, 0.5
- cart_creations = 50000 × 0.20 = 10,000
- abandoned_carts = 10000 × 0.70 = 7,000
- lost_revenue = 7000 × 80 = $560,000
- recoverable = 560000 × 0.08 = $44,800
- recovery_cost = 7000 × 0.5 = $3,500
- net_gain = 44800 - 3500 = $41,300
- ROI = 44800 / 3500 = **1280% → 🟢 Good**

- [ ] **Step 1: Write failing tests (canonical + 3 health bands + defensive clamp + edge)**

Create `tests/cart-abandonment-cost-calculator.test.ts`:

```ts
import { test } from 'node:test';
import assert from 'node:assert/strict';
import {
  calcRecoveryROI,
  calcHealthBand,
} from '../src/engines/marketing/cart-abandonment-cost-calculator';

// Canonical: 50000, 20, 70, 80, 8, 0.5 → 1280% ROI → 🟢 Good
test('cart-abandonment: canonical inputs → 1280% ROI → 🟢 Good', () => {
  const traffic = 50000;
  const addRate = 20;
  const abandonRate = 70;
  const aov = 80;
  const recoveryRate = 8;
  const recoveryCost = 0.5;
  const creations = traffic * (addRate / 100);
  const abandoned = creations * (abandonRate / 100);
  const lost = abandoned * aov;
  const recoverable = lost * (recoveryRate / 100);
  const cost = abandoned * recoveryCost;
  const roi = cost > 0 ? recoverable / cost : 0;
  assert.ok(Math.abs(roi - 12.80) < 0.01, `expected 12.80, got ${roi.toFixed(2)}`);
  assert.equal(calcHealthBand(roi), 'good');
});

// Health bands: 3 boundary tests
test('cart-abandonment: ROI ≥ 300% (3.0x) → good', () => {
  assert.equal(calcHealthBand(3.0), 'good');
  assert.equal(calcHealthBand(5.0), 'good');
});

test('cart-abandonment: ROI 100-300% (1.0-3.0x) → warning', () => {
  assert.equal(calcHealthBand(2.0), 'warning');
  assert.equal(calcHealthBand(1.0), 'warning');
});

test('cart-abandonment: ROI < 100% (<1.0x) → critical', () => {
  assert.equal(calcHealthBand(0.5), 'critical');
  assert.equal(calcHealthBand(0.0), 'critical');
});

// Defensive clamp
test('cart-abandonment: negative traffic clamps to 0 (defensive layer 2)', () => {
  // clampNonNegative(-50000) → 0; calcRecoveryROI(0, ...) → 0
  const roi = calcRecoveryROI(0, 20, 70, 80, 8, 0.5);
  assert.equal(roi, 0);
  assert.ok(roi >= 0);
});

// Edge case
test('cart-abandonment: all-zero inputs → guard handles gracefully', () => {
  const roi = calcRecoveryROI(0, 0, 0, 0, 0, 0);
  assert.ok(roi >= 0 || isNaN(roi));
});
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `node --import tsx tests/cart-abandonment-cost-calculator.test.ts`
Expected: FAIL with "Cannot find module".

- [ ] **Step 3: Implement engine with full pattern**

Create `src/engines/marketing/cart-abandonment-cost-calculator.ts` following roas-calculator.ts structure with cart-abandonment math + 8 outputs + recovery_roi health band.

CustomFn prepends `var cnn=...` and wraps all 6 input-parses.

- [ ] **Step 4: Verify tests pass**

Run: `node --import tsx tests/cart-abandonment-cost-calculator.test.ts`
Expected: 6/6 pass.

- [ ] **Step 5: Verify customFn parses**

Run: `node tests/scripts/verify-customfn.mjs cart-abandonment-cost-calculator`
Expected: `OK (N chars)`.

- [ ] **Step 6: Regenerate staticExamples[0]**

Run: `node scripts/codegen-examples.mjs`
Expected: staticExamples[0] updated.

- [ ] **Step 7: Add import + export to marketing/index.ts**

Modify `src/engines/marketing/index.ts`:
```ts
// Add at end:
export * from './cart-abandonment-cost-calculator';
```

Add header comment:
```ts
// P16-2 cart-abandonment-cost-calculator.ts -- added in P16-2
```

- [ ] **Step 8: Verify build succeeds**

Run: `pnpm exec astro build 2>&1 | tail -5`
Expected: 311 static pages built (309 + 2 new).

- [ ] **Step 9: Verify HTML5 layer on new pages**

Run: `node scripts/smoke-html5.mjs --strict`
Expected: exit 0.

- [ ] **Step 10: Commit**

```bash
git add src/engines/marketing/cart-abandonment-cost-calculator.ts tests/cart-abandonment-cost-calculator.test.ts src/engines/marketing/index.ts
git commit -m "feat(p16-2): cart-abandonment-cost-calculator — M: 7→8 (engine 100, milestone)"
```

---

### Task 3: Old-pattern sweep Batch 1 — cost(4) + freelance(3) + hiring-team(6) = 13 engines

**Files:**
- Modify: 13 engine files (4 cost + 3 freelance + 6 hiring-team)
- Modify: 13 corresponding test files

**Interfaces:**
- Consumes: `clampNonNegative` from `../../core/engines/helpers`
- Produces: 13 engines with clamp in calculate()/generate() + `var cnn=...` inline in customFn + +1 defensive test per engine

**⚠️ Sample-verify BEFORE sweep**: Pick 1 BIZ engine (e.g., `meeting-cost-calculator` from cost/) and confirm:
- It uses inline `clientConfig: { customFn }` pattern (consistent with meeting-cost-calculator.ts:227-231)
- `var cnn=function(x){return Math.max(0,x)};` is NOT already present
- HTML5 layer is already in place at line 940 (no work needed for HTML5)

If any engine ALREADY has cnn (likely hiring-team/ P11 — already swept by P14-Followup), skip duplicate work.

- [ ] **Step 1: Sample-verify 1 engine per category**

Run:
```bash
# Cost (4)
for f in cost/{employee-cost,meeting-cost,productivity-score,remote-vs-office}-calculator.ts; do
  echo "=== $f ==="
  grep -c "var cnn\|clampNonNegative" "src/engines/$f" || echo "0"
done
```

Expected: All 4 cost engines show 0 (need full sweep).

```bash
# Freelance (3)
for f in freelance/{affiliate-income,freelance-rate,hourly-vs-fixed}-calculator.ts; do
  echo "=== $f ==="
  grep -c "var cnn\|clampNonNegative" "src/engines/$f" || echo "0"
done
```

Expected: All 3 freelance engines show 0 (need full sweep).

```bash
# Hiring-team (6) — P11 era, may already have cnn from P14-Followup
for f in hiring-team/{attrition-cost,comp-banding,equity-refresh,fully-loaded-employee-cost,productivity-ramp-curve,time-to-productivity}-calculator.ts; do
  echo "=== $f ==="
  grep -c "var cnn\|clampNonNegative" "src/engines/$f" || echo "0"
done
```

For each engine that already shows cnn+clamp (likely 6/6 hiring-team), SKIP the sweep and only add the defensive test.

- [ ] **Step 2: For each of 13 engines, add clamp pattern**

For each engine file (or skip if already done):

1. Add import after existing imports:
```ts
import { clampNonNegative } from '../../core/engines/helpers';
```

2. In `calculate()` or `generate()`, wrap each `parseFloat(inputs.X) || 0` or `Number(inputs.X) || 0` with `clampNonNegative(...)`. Example:
```ts
// Before:
const attendees = parseFloat(inputs.attendees) || 0;
// After:
const attendees = clampNonNegative(parseFloat(inputs.attendees) || 0);
```

3. In customFn string (typically `const customFn = "..."` or `clientConfig: { customFn: "..." }`):
   - Prepend `var cnn=function(x){return Math.max(0,x)};`
   - Wrap each input-parse: `parseFloat(inputs.attendees)||0` → `cnn(parseFloat(inputs.attendees)||0)`

Use Edit (not Write). Read each file first to confirm structure before editing.

- [ ] **Step 3: For each of 13 engines, add +1 defensive test**

For each test file, append a +1 test verifying post-clamp math behavior:

```ts
test('{slug}: negative {input_name} clamps to 0 (defensive layer 2)', () => {
  // Per P16 sweep: clampNonNegative guard prevents negative-ratio output
  const result = generate({ /* relevant inputs */ });
  assert.ok(Array.isArray(result));
  assert.ok(!result.some(line => /NaN|Infinity|-/.test(line)));
});
```

Adjust per engine's actual `generate()` signature and inputs.

- [ ] **Step 4: Verify all 13 customFns parse**

Run: `node tests/scripts/verify-customfn.mjs employee-cost-calculator meeting-cost-calculator productivity-score remote-vs-office-calculator affiliate-income-calculator freelance-rate-calculator hourly-vs-fixed-calculator attrition-cost-calculator comp-banding-calculator equity-refresh-calculator fully-loaded-employee-cost-calculator productivity-ramp-curve-calculator time-to-productivity-calculator`

Expected: 13/13 print `OK (N chars)`.

- [ ] **Step 5: Run codegen drift check**

Run:
```bash
node scripts/codegen-examples.mjs --check
node scripts/codegen-customfn.mjs --check
```

Expected: PASS (no drift).

- [ ] **Step 6: Run new defensive tests**

Run:
```bash
node --import tsx tests/cost/{employee-cost,meeting-cost,productivity-score,remote-vs-office}-calculator.test.ts
node --import tsx tests/freelance/{affiliate-income,freelance-rate,hourly-vs-fixed}-calculator.test.ts
node --import tsx tests/hiring-team/{attrition-cost,comp-banding,equity-refresh,fully-loaded-employee-cost,productivity-ramp-curve,time-to-productivity}-calculator.test.ts
```

Expected: All 13 test files pass (existing tests + new defensive test).

- [ ] **Step 7: Commit per category (3 commits)**

```bash
git add src/engines/cost/ tests/cost/
git commit -m "feat(p16-3a): cost old-pattern sweep — 4 engines + 4 tests (clamp + cnn)"

git add src/engines/freelance/ tests/freelance/
git commit -m "feat(p16-3b): freelance old-pattern sweep — 3 engines + 3 tests"

git add src/engines/hiring-team/ tests/hiring-team/
git commit -m "feat(p16-3c): hiring-team old-pattern sweep — 6 engines + 6 tests (verify pre-existing cnn)"
```

---

### Task 4: Old-pattern sweep Batch 2 — investment(5) + real-estate(6) + saas(5) = 16 engines

**Files:**
- Modify: 16 engine files (5 investment + 6 real-estate + 5 saas)
- Modify: 16 corresponding test files

**Interfaces:** Same as Task 3.

- [ ] **Step 1: Sample-verify 1 engine per category**

```bash
# Investment (5)
for f in investment/{compound-interest,equity-dilution,freelance-tax,sponsorship-rate,time-value}-calculator.ts; do
  echo "=== $f ==="
  grep -c "var cnn\|clampNonNegative" "src/engines/$f" || echo "0"
done

# Real-estate (6) — P5 era, may already have cnn from P14-Followup
for f in real-estate/{brrrr,cap-rate,dscr,mortgage,rental-yield,rent-vs-buy}-calculator.ts; do
  echo "=== $f ==="
  grep -c "var cnn\|clampNonNegative" "src/engines/$f" || echo "0"
done

# SaaS (5)
for f in saas/{burn-rate,churn-rate,market-size-estimator,mrr,revenue-projector}-calculator.ts; do
  echo "=== $f ==="
  grep -c "var cnn\|clampNonNegative" "src/engines/$f" || echo "0"
done
```

- [ ] **Step 2: For each of 16 engines, add clamp pattern**

Same as Task 3 Step 2. Skip engines that already have cnn+clamp.

- [ ] **Step 3: For each of 16 engines, add +1 defensive test**

Same as Task 3 Step 3.

- [ ] **Step 4: Verify all customFns parse**

Run: `node tests/scripts/verify-customfn.mjs compound-interest-calculator equity-dilution-calculator freelance-tax-calculator sponsorship-rate-calculator time-value-calculator brrrr-calculator cap-rate-calculator dscr-calculator mortgage-calculator rental-yield-calculator rent-vs-buy-calculator burn-rate-calculator churn-rate-calculator market-size-estimator mrr-calculator revenue-projector`

Expected: 16/16 print `OK (N chars)`.

- [ ] **Step 5: Run codegen drift check**

```bash
node scripts/codegen-examples.mjs --check
node scripts/codegen-customfn.mjs --check
```

Expected: PASS.

- [ ] **Step 6: Run new defensive tests**

Run per category. Expected: All 16 test files pass.

- [ ] **Step 7: Commit per category (3 commits)**

```bash
git add src/engines/investment/ tests/investment/
git commit -m "feat(p16-4a): investment old-pattern sweep — 5 engines + 5 tests"

git add src/engines/real-estate/ tests/real-estate/
git commit -m "feat(p16-4b): real-estate old-pattern sweep — 6 engines + 6 tests"

git add src/engines/saas/ tests/saas/
git commit -m "feat(p16-4c): saas old-pattern sweep — 5 engines + 5 tests"
```

---

### Task 5: Old-pattern sweep Batch 3 — valuation(13) engines

**Files:**
- Modify: 13 engine files in `src/engines/valuation/`
- Modify: 13 corresponding test files

**Interfaces:** Same as Tasks 3-4.

- [ ] **Step 1: Sample-verify 1 engine**

```bash
for f in valuation/{arr-multiple-valuation,break-even,burn-multiple-rule-of-40,cac,course-pricing,email-list-revenue,ltv,project-profitability,saas-pricing-planner,saas-valuation,safe-convertible-note,stripe-fee,unit-economics}-calculator.ts; do
  echo "=== $f ==="
  grep -c "var cnn\|clampNonNegative" "src/engines/$f" || echo "0"
done
```

- [ ] **Step 2: For each of 13 engines, add clamp pattern**

Same as Task 3 Step 2. Skip engines that already have cnn+clamp.

- [ ] **Step 3: For each of 13 engines, add +1 defensive test**

Same as Task 3 Step 3.

- [ ] **Step 4: Verify all customFns parse**

Run: `node tests/scripts/verify-customfn.mjs arr-multiple-valuation-calculator break-even-calculator burn-multiple-rule-of-40-calculator cac-calculator course-pricing-calculator email-list-revenue-calculator ltv-calculator project-profitability-calculator saas-pricing-planner saas-valuation-calculator safe-convertible-note-calculator stripe-fee-calculator unit-economics-calculator`

Expected: 13/13 print `OK (N chars)`.

- [ ] **Step 5: Run codegen drift check**

```bash
node scripts/codegen-examples.mjs --check
node scripts/codegen-customfn.mjs --check
```

Expected: PASS.

- [ ] **Step 6: Run new defensive tests**

Run per engine. Expected: All 13 test files pass.

- [ ] **Step 7: Commit (1 commit for batch)**

```bash
git add src/engines/valuation/ tests/valuation/
git commit -m "feat(p16-5): valuation old-pattern sweep — 13 engines + 13 tests"
```

---

### Task 6: P15 polish bundle (4 items, single commit)

**Files:**
- Modify: `tests/baselayout-clerk-script.test.ts` (env skip-marker)
- Modify: `tests/acv-calculator.test.ts` (band verdict assertion)
- Modify: `tests/funnel-step-calculator.test.ts` (replace manual filteredSteps with generate())
- Modify: `tests/article-helpfulness-calculator.test.ts` (band cascade assertion)
- Modify: `src/core/engines/helpers.ts:72` (remove `as number` cast)
- Modify: `tests/helpers.test.ts` (rename Test 5)
- Modify: `src/engines/customer-support/first-response-time-calculator.ts` (extract LIFT_PERCENT constant)

- [ ] **Step 1: Add env-test skip-marker**

In `tests/baselayout-clerk-script.test.ts`, find first test. Add at top of test function body:
```ts
test('...', () => {
  if (!process.env.PUBLIC_CLERK_PUBLISHABLE_KEY) return; // skip when env missing
  // ... existing test code ...
});
```

Verify: `node --import tsx tests/baselayout-clerk-script.test.ts` skips when env missing.

- [ ] **Step 2: Improve acv-calculator test (band verdict)**

In `tests/acv-calculator.test.ts`, find the P14-Followup defensive test (Test N+1 typically). Add a band verdict assertion:
```ts
assert.equal(calcHealthBand(baseACV(0, 12)), 'critical');
```

Adjust per actual `calcHealthBand` signature in `src/engines/sales/acv-calculator.ts`.

- [ ] **Step 3: Improve funnel-step-calculator test (generate() call)**

In `tests/funnel-step-calculator.test.ts`, find the P14-Followup defensive test. Replace manual `filteredSteps = [...]` with `generate()` call:
```ts
test('funnel-step: negative step1 clamps to 0 → funnelEndToEnd handles', () => {
  const result = generate({ step1: '-1000', step2: '800', step3: '500', step4: '320', step5: '210' });
  assert.ok(Array.isArray(result));
  assert.ok(result.length > 0);
  assert.ok(!result.some(line => /NaN|Infinity|-/.test(line)));
});
```

Adjust per actual `generate` signature in `src/engines/product-analytics/funnel-step-calculator.ts`.

- [ ] **Step 4: Improve article-helpfulness test (band cascade)**

In `tests/article-helpfulness-calculator.test.ts`, find the P14-Followup defensive test. Add:
```ts
assert.equal(calcHealthBand(0, 0), 'critical'); // both inputs clamped to 0
```

Adjust per actual `calcHealthBand(helpful, voteRate)` 2-arg signature in `src/engines/knowledge/article-helpfulness-calculator.ts`.

- [ ] **Step 5: Remove no-op cast in helpers.ts**

Edit `src/core/engines/helpers.ts:72`:
```ts
// Before:
return Number.isNaN(x as number) ? 0 : Math.max(0, x);
// After:
return Number.isNaN(x) ? 0 : Math.max(0, x);
```

- [ ] **Step 6: Rename Test 5 in helpers.test.ts**

Edit `tests/helpers.test.ts`:
```ts
// Before:
test('clampNonNegative: undefined → 0 (defensive, via typeof guard)', () => {
// After:
test('clampNonNegative: undefined → NaN (caller-must-pre-validate)', () => {
```

- [ ] **Step 7: Extract LIFT_PERCENT constant**

In `src/engines/customer-support/first-response-time-calculator.ts`:
1. Add `const LIFT_PERCENT = 5;` at top of file (after imports).
2. In `calculate()`, replace `+ 5` literal with `+ LIFT_PERCENT`.
3. In customFn string, prepend `var LIFT_PCT=5;` then replace `+ 5` with `+ LIFT_PCT`.

Verify: `node tests/scripts/verify-customfn.mjs first-response-time-calculator` parses OK.

- [ ] **Step 8: Run all modified tests**

Run:
```bash
node --import tsx tests/baselayout-clerk-script.test.ts
node --import tsx tests/acv-calculator.test.ts
node --import tsx tests/funnel-step-calculator.test.ts
node --import tsx tests/article-helpfulness-calculator.test.ts
node --import tsx tests/helpers.test.ts
node --import tsx tests/first-response-time-calculator.test.ts
```

Expected: All 6 test files pass.

- [ ] **Step 9: Commit**

```bash
git add tests/baselayout-clerk-script.test.ts tests/acv-calculator.test.ts tests/funnel-step-calculator.test.ts tests/article-helpfulness-calculator.test.ts src/core/engines/helpers.ts tests/helpers.test.ts src/engines/customer-support/first-response-time-calculator.ts
git commit -m "chore(p16-6): P15 polish bundle — env skip-marker + 3 test improvements + helpers cleanup + LIFT_PERCENT"
```

---

### Task 7: Holistic ship (memory + MEMORY.md + 3-way sync)

**Files:**
- Create: `memory/p16-100-milestone-shipped.md`
- Modify: `memory/MEMORY.md` (in-repo)
- Modify: `C:\Users\元始天尊\.claude\projects\D--E-----youtube-tools\memory\MEMORY.md` (Claude-side mirror)

- [ ] **Step 1: Run full test suite**

Run: `SKIP_PRECOMMIT_CHECK=1 pnpm check`
Expected: All P16 per-task tests pass; env-dependent tests skip cleanly.

- [ ] **Step 2: Run codegen + smoke verification**

Run:
```bash
node scripts/codegen-examples.mjs --check
node scripts/codegen-customfn.mjs --check
pnpm exec astro build
node scripts/smoke-html5.mjs --strict
node tests/scripts/verify-customfn.mjs $(ls src/engines/**/*.ts | grep -v index.ts | sed 's|src/engines/||;s|\.ts$||' | tr '\n' ' ')
```

Expected:
- codegen-examples: 100/100 PASS
- codegen-customfn: PASS
- astro build: 311 pages
- smoke-html5: exit 0
- verify-customfn: 100/100 print `OK (N chars) [style-name]`

- [ ] **Step 3: Confirm engine count = 100**

Run: `ls src/engines/**/*.ts | grep -v index.ts | wc -l`
Expected: 100

- [ ] **Step 4: Write P16 ship memory**

Create `memory/p16-100-milestone-shipped.md` with sections:
- **Batch summary** — table (engine count 98→100, 7 commits, ~1480 LOC, ~95 files)
- **Tasks DONE** — Tasks 1-7 status table
- **What shipped** — 2 new engines + 42-engine sweep + 4 P15 polish items + tooling
- **Defense layer coverage** — HTML5 (100/100) + programmatic (100/100) + customFn (100/100)
- **Lessons** — consolidate from P15 + P16 (BIZ_CONFIG misnomer; HTML5 already in place; P-series overlap)
- **Maintenance mode trigger events** — copy from spec §10

- [ ] **Step 5: Update MEMORY.md (both in-repo and Claude-side)**

Add one-line pointer to `memory/MEMORY.md`:
```markdown
- [P16 100-Milestone shipped](p16-100-milestone-shipped.md) — 7 commits; 2 new e-commerce engines (M: 6→8) + 42-engine old-pattern sweep + 4 P15 polish items; defense layer 100/100 coverage; ready for maintenance mode
```

Mirror to `C:\Users\元始天尊\.claude\projects\D--E-----youtube-tools\memory\MEMORY.md`.

- [ ] **Step 6: Dual-push**

```bash
git push origin HEAD
git push github HEAD
```

If GitHub push blocks: `SKIP_PUSH_FETCH=1 git push github HEAD`

Verify: `git rev-list --left-right --count origin/master...github/master` → `0	0`

- [ ] **Step 7: Commit memory**

```bash
git add memory/
git commit -m "docs(p16): ship memory + MEMORY.md dual-sync (100 engines milestone + maintenance mode)"
git push origin HEAD
SKIP_PUSH_FETCH=1 git push github HEAD
```

---

## Self-Review

**1. Spec coverage:**
- §1 Problem Statement → Tasks 1-7 (all 5 defer items addressed)
- §2 Decisions → Task class assignments (mechanical = 1 reviewer, integration = 1+1) + per-batch split (Tasks 3-5)
- §3 Architecture → Tasks 1-2 (new engines with full pattern) + Tasks 3-5 (programmatic + customFn layers)
- §4.1 New engines → Tasks 1-2 (coupon + cart-abandonment)
- §4.2 Old-pattern sweep → Tasks 3-5 (3 batches: 13 + 16 + 13)
- §4.3 P15 polish → Task 6 (4 items)
- §4.4 Holistic ship → Task 7
- §5 Data flow → Inline in each task step
- §6 Testing → Task 7 (cross-cutting verification)
- §7 Out of scope → Addressed in spec; not in plan
- §8 Rollout → Task 7 (dual-push + memory)
- §9 Risk → Sample-verify per category (Tasks 3-5 Step 1); abort criteria in each task

**2. Placeholder scan:** No "TBD", "TODO", "implement later". All steps have concrete commands or code.

**3. Type consistency:** `clampNonNegative(x: number): number` defined in helpers.ts:67; referenced consistently. `var cnn=function(x){return Math.max(0,x)};` pattern from P14-Followup reused verbatim. `calcHealthBand(roi)` returns 'good'/'warning'/'critical' (consistent with P9-P14 engines). `calcTrueROI` / `calcRecoveryROI` / `calcHealthBand` exported per engine (matches roas-calculator pattern).

**4. Engine count verification:** 98 (P15 ship) + 2 new = 100. Pre-flight ls confirmed: cost 4 + freelance 3 + hiring-team 6 + investment 5 + real-estate 6 + saas 5 + valuation 13 = 42 old-pattern engines.

**5. Critical correction from spec:** Spec §3 stated "BIZ_CONFIG form rendering at line 1387-1454". **Empirically verified via grep**: line 1387-1454 contains visual family configs (icon colors for chart legends), NOT form rendering. All engines use the SAME Astro template at line 928-943 which ALREADY has `step="any" min="0"`. **HTML5 layer needs NO work for Tasks 3-5** — only programmatic + customFn layers. This is a significant simplification of the sweep work.

**6. Plan gaps:**
- Task 3-5 Step 1 sample-verify may find that some engines (hiring-team P11, real-estate P5) already have cnn+clamp from P14-Followup. Implementer should skip duplicate work for those engines.
- Task 6 has 4 separate polish items in one commit — acceptable since each is <10 LOC and pre-tested.
- Task 7 Step 2 runs `verify-customfn.mjs` for ALL 100 engines — large invocation but acceptable for final verification.

**7. Adjustment made during self-review:**
- Removed HTML5 layer work from Tasks 3-5 (per empirical finding: already in place)
- Task 3 Step 1 explicit sample-verify to detect pre-existing cnn in P11/P5 categories
- Task 7 Step 2 added full 100-engine verify-customfn sweep as acceptance criterion
- Task 7 Step 3 explicit engine-count verification (catches miscount)

**Final structure:** 7 tasks. Matches spec's 7-task scope.
